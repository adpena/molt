# Solo-Owner Lane Claims

Some lanes must be driven **END-TO-END by ONE agent**. Splitting them across
agents causes exactly the failures this collaboration has hit repeatedly:
colliding edits, trampled landings, and *masked frontiers* (one agent's local
state hiding the real blocker from another). Those lanes are **SOLO lanes**.

Before working a SOLO lane you MUST claim it here. The claim is a git-atomic
lock: because claims are landed by fast-forward (`tools/ff_land.py`), exactly one
agent can win a claim race — the loser's push is refused and it backs off.

This registry is authoritative at `origin/main`. Read it there, not from a stale
checkout (`python tools/tree_drift_check.py --fetch` first).

## SOLO lanes (claim before starting)

- **`E1-WITNESS-TO-GREEN`** — the entire witness closure, owned end-to-end by one
  agent: seal-regen → WASM link → numpy static-lib link → execution →
  `candidate_outputs.npz` → `check_parity.py` PASS. Do NOT split this across
  agents; the seal artifacts, cpython-abi link, and acceptance lane are one
  coherent arc. (See the E1 order-of-operations in the PAUSE HANDOFF /
  `ORCHESTRATION.md`.)

- **`CODEX-B-CACHE-KEY`** — frontend lowering cache-KEY scoping (`cache_fingerprints.py`
  + `frontend_*` + aligned tests) so unrelated CLI/runtime/link edits don't cold-start
  the witness lowering cache. Coherent structural move; one owner. (Orchestrator-held;
  handoff from the prior in-progress agent per the reclaim directive in `ORCHESTRATION.md`.)

- **`REVIEW-5-SCCP-STR-UNICODE`** — adversarial-review finding #5 (P1
  CODEX-CORRECTNESS): SCCP constant-folding of str builtins/methods must use
  CPython code-point semantics, never Rust UTF-8 byte offsets. Single authority:
  `runtime/molt-passes/src/tir/passes/sccp/eval.rs`
  (`eval_concrete_builtin`/`eval_concrete_method`). One owner so the fix + teeth
  land as a coherent cut.

(Add new SOLO lanes here as the orchestrator or a claimant identifies them.)

## Protocol (binding)

**Helper (does steps 1-3 correctly for you):** `python tools/claim_lane.py <lane>
--check` (read-only; exit 0 = claimable, 1 = held → back off) and
`python tools/claim_lane.py <lane> --claim --agent <id> --note "..."` (pre-checks,
appends the row, `ff_land`s, and backs off on a lost race). Log progress/finish
with `--append PROGRESS|COMPLETE|RELEASED --agent <id> --note "..."`. The manual
steps below are the contract it implements.

1. **Drift-sweep first.** `git fetch origin`; read THIS file at current
   `origin/main`. `python tools/tree_drift_check.py --fetch` to confirm your tree
   isn't masking.
2. **Check the claim.** If the SOLO lane you want is `CLAIMED` and not STALE (see
   §6), **BACK OFF** — pick a different unclaimed SOLO lane, or a standing lane
   (Codex B/C/D). Never work a lane another agent holds.
3. **Claim it.** Append ONE row to the log (lane, your agent-id, UTC ISO
   timestamp, `CLAIMED`, first-step note) and land it with
   `python tools/ff_land.py`. Because landing is a fast-forward, exactly ONE
   claim wins a race. **If `ff_land` REFUSES (non-ff), someone moved first** —
   `git fetch`, re-read this file; if your lane is now claimed, discard your claim
   row and BACK OFF.
4. **Own it end-to-end.** While your claim is active, no other agent touches the
   lane's files, artifacts, or tests. Post append-only `PROGRESS` rows (with
   evidence: queue run ids, commits) at least every few hours so the claim is
   visibly alive.
5. **Final completion is NOT a green build.** Mark `COMPLETE` only after ALL of:
   - **(a) FINAL EXIT CRITERIA met, with evidence.** For `E1-WITNESS-TO-GREEN`:
     `candidate_outputs.npz` produced through Molt WASM AND `check_parity.py`
     PASS — cite the `pact-witness-acceptance` queue run id and paste the parity
     verdict. Zero fakes, no host-CPython/Pyodide fallback, POISON-clean.
   - **(b) RECURSIVE ADVERSARIAL REVIEW accomplished.** You (or a reviewer you
     spawn) actively tried to REFUTE the result from independent angles
     (re-run on a clean origin/main worktree; probe determinism/tolerances;
     confirm no masking, no fake symbol, no weakened assert) and it SURVIVED.
     Record what you tried and why it held.
   - **(c) SENIOR-ENGINEER REVIEW green.** Correctness incl. memory safety;
     performance (faster than CPython on the claimed target, honest numbers);
     CPython ≥3.12 compatibility in the verified subset; fidelity; no
     duplicate-authority / no partials. Record the sign-off.
   Only then append a `COMPLETE` row with the evidence links and `ff_land` it.
6. **Stale / release.** A claim with an explicit `RELEASED` row may be reclaimed.
   Otherwise a claim is STALE **only if BOTH** (a) no `PROGRESS` row for >4h AND
   (b) an OBJECTIVE-LIVENESS check shows no real activity. A missing PROGRESS row
   is NOT sufficient — an active agent often works silently in its worktree/queue
   without touching this log (this cost a wrong reclaim 2026-07-08). Before landing
   a `RECLAIM` row you MUST verify NO objective activity in the last ~2h:
   - the claimant's worktree commit log: `git -C <worktree> log --oneline --since="3 hours ago"`;
   - recent proof-queue rows for the lane: `tools/proof_queue.py status` (+ any
     worktree-local `logs/proof_queue`);
   - recent `origin/main` commits by that agent.
   If ANY show recent activity, the claim is ALIVE — do NOT reclaim (post a
   PROGRESS observation instead and ping the claimant to log its own progress).
   Cite the objective-liveness evidence (or its absence) in the `RECLAIM` row.
   Never silently take over a live claim; escalate to the orchestrator if contested.
   **Claimants:** post a `PROGRESS` row (or `python tools/claim_lane.py <lane>
   --append PROGRESS ...`) at least every few hours — silence forces others to
   guess your liveness from commits/queue, which is fragile.

## Log (append-only; newest at bottom; land each row via `ff_land`)

| lane | agent-id | UTC (ISO) | status | note / evidence |
|------|----------|-----------|--------|-----------------|
| _(none yet — first claimant of E1-WITNESS-TO-GREEN appends here)_ | | | | |
| E1-WITNESS-TO-GREEN | Codex/codex-e1-seal-regen-20260707 | 2026-07-07T21:02:53Z | CLAIMED | Start from clean origin/main 6e1494cb8; first step is seal-regen artifact custody, re-seal gate, then pact-witness-acceptance. |
| E1-WITNESS-TO-GREEN | orchestrator (obs) | 2026-07-08T01:20:00Z | PROGRESS | Orchestrator-observed ALIVE: codex-e1 worktree active (14 dirty, static_truth/analysis_collect_static <2h), seal machinery landing (8e4fae62b skip-stale, 36b11f58f relativize). Claim is LIVE — do NOT reclaim. E1 agent: post your own PROGRESS rows + drive the 17-source seal regen (still 0 runtime_python_import_modules on disk). |
| CODEX-B-CACHE-KEY | orchestrator | 2026-07-08T01:35:00Z | CLAIMED | Reclaimed per operator direction. Awaiting handoff of the in-progress cache_fingerprints.py tooling-scoping WIP (bank to wip/codex-b-cache-scoping-20260708), then finish with warm-run before/after cache-hit measurement + tests. |
| CODEX-B-CACHE-KEY | orchestrator | 2026-07-08T02:05:00Z | COMPLETE | Landed b9d6963fb. Frontend lowering cache keyed on semantic tooling (33 post-lowering cli files excluded); before: unrelated edit INVALIDATES -> ~180-250s re-lower; after: REUSES. Correctness fix: external_native kept in scope (feeds direct_call_modules) + regression test. 7/7 teeth. Senior-reviewed + independently reproduced. |
| E1-WITNESS-TO-GREEN | orchestrator | 2026-07-08T09:39:33Z | RECLAIM | Reclaimed: claim STALE (last progress 2026-07-08T01:20Z, now 09:38Z = 8h; seal still 0 runtime_python_import_modules; recent worktree touches are phase-split ripple, not seal progress). Orchestrator drives the 17-source seal regen to E1 green. |
| E1-WITNESS-TO-GREEN | orchestrator (correction) | 2026-07-08T09:55:00Z | PROGRESS | CORRECTION: my 09:38 stale-reclaim was WRONG — the E1 Codex agent is ACTIVE and PAST THE SEAL. It has a working witness build (app.wasm, run 20260708T092032) that passes seal custody + WASM link (89e5160ea) + instantiation (its manifest link-import host-trap fix 985567256 cleared the libc++ bad_function_call frontier). NEW frontier = reserved-callable dispatch: molt_call_indirect4 idx=2108 molt_type_new arity mismatch (expected 5 got 4). Codex agent RETAINS witness E2E ownership; the molt_type_new reserved-callable frontier is ORCHESTRATOR-owned — orchestrator drives it. E1 agent: post PROGRESS rows so this doesn't recur. |
| E1-WITNESS-TO-GREEN | orchestrator (correction) | 2026-07-08T10:53:27Z | PROGRESS | FRONTIER ADVANCED past molt_type_new. pact-witness-acceptance 4521da6aebe64770 fails unsupported-direct-call 'AxisError' at scipy/_lib/_util.py:6:14. Root: call_dispatch_named.py:2184-2237 — names imported from a SOURCE-COMPILED package in the witness closure (numpy) fail closed (Tier0 non-allowlisted) instead of resolving through package symbol-closure; only imported_from is None gets _emit_dynamic_call. POISON-clean fix: imported-from-compiled-package callables resolve through package/import symbol-closure custody (numpy compiled here), never Molt-baked. Driving via orchestrator worktree-isolated agent. |
| E1-WITNESS-TO-GREEN | orchestrator (correction) | 2026-07-08T11:00:14Z | PROGRESS | E1 FRONTIER (past molt_type_new): pact-witness-acceptance 4521da6aebe64770 fails unsupported-direct-call 'AxisError' at scipy/_lib/_util.py:6. ROOT CAUSE ISOLATED (reproduced at frontend level, no build): scipy does 'from numpy.exceptions import AxisError'; the CHILD module numpy.exceptions is NOT in the witness import closure (known_modules), so the bare-name call fails closed. Verified: known_modules={numpy} alone -> RAISES; known_modules={numpy,numpy.exceptions} -> OK (call_bind). The fail-closed is CORRECT — do NOT loosen call_dispatch_named.py:2231 to emit call_bind for unknown modules (that is a fail-OPEN POISON: binds to an undiscovered module -> resolves to nothing at runtime). REAL FIX is upstream in numpy closure discovery: numpy.exceptions (and siblings) must be admitted into known_modules via package/import symbol-closure custody. Ties to the stale numpy seal (0 runtime_python_import_modules). E1 SOLO-lane witness-closure work, needs full build loop. |
| REVIEW-5-SCCP-STR-UNICODE | Claude/claude-review5-sccp-unicode-20260708 | 2026-07-08T17:23:09Z | CLAIMED | Finding #5 (P1 CODEX-CORRECTNESS). Verified free (no origin/main sccp touches in 2d; no other claim). Root: sccp/eval.rs folds len/find/rfind/count("")/zfill with Rust UTF-8 byte semantics -> silent miscompile of every non-ASCII string constant. Fix in flight: code-point semantics at the single fold authority + non-ASCII teeth (CPython 3.12-captured expectations). |
| REVIEW-5-SCCP-STR-UNICODE | Claude/claude-review5-sccp-unicode-20260708 | 2026-07-08T17:30:42Z | COMPLETE | Landed 9ebc5f4752. sccp/eval.rs now folds str len/find/rfind/count("")/zfill with CPython code-point semantics (chars().count() + shared byte_offset_to_char_index helper); zfill also made negative-width-safe (old `*w as usize` wrapped -> compile-time OOM/panic). Single authority; no duplicate fold path (grep-verified value_range/ir clean). Teeth: sccp::eval::unicode_fold_tests (6 tests, non-ASCII incl. astral, CPython 3.12-captured). Queue run 20260708T172531-review5-sccp-unicode-1f6587e47637432e PASSED rc=0: 46 sccp tests pass. rustfmt --changed clean. Every expected value re-verified against the CPython 3.12 reference interpreter (primary source). |
| E1-SCIPY-RANK-FILTER-1D | claude-opus-e1-rankfilter | 2026-07-08T18:08:31Z | CLAIMED | Building scipy.ndimage._rank_filter_1d (_rank_filter_1d.cpp) to wasm32 static-link artifact via source-extension custody lane |
| E1-NUMPY-HEADER-OVERLAY-CUSTODY | claude-opus-e1-numpy-header | 2026-07-08T18:09:23Z | CLAIMED | Route numpy headers through package custody (_core/include); delete include/numpy overlay; decrement fail_closed numpy-header poison class |
| C4 | Codex/codex-c4-scoreboard-gate-20260708 | 2026-07-08T18:12:37Z | CLAIMED | Claim C4 R8 scoreboard/release-gate lane; first step wire canonical perf scoreboard freshness+greenness into the existing freshness gate and prove it with focused tests. |
| E1-SCIPY-NI-SUPPORT | Codex/codex-e1-scipy-ni-support-20260708 | 2026-07-08T18:28:01Z | CLAIMED | Build scipy.ndimage._ni_support into wasm32 static-link artifact through source-extension custody; no package-specific regen or header overlay. |
| E1-SCIPY-NI-DOCSTRINGS | Codex/codex-e1-scipy-ni-docstrings-20260708 | 2026-07-08T19:46:28Z | CLAIMED | Build/prove scipy.ndimage._ni_docstrings source-extension artifact or closure-strippable outcome; no package-specific overlay. |
| REVIEW-9-PARITY-IMPORTERROR-STRICT | Codex/codex-review9-parity-importerror-20260708 | 2026-07-08T20:01:49Z | CLAIMED | Fix review finding #9: strict parity gate must not skip Molt-only ImportError-shaped failures; first step inspect parity_gate authority and tests, then land fail-closed behavior with teeth. |
| REVIEW-9-PARITY-IMPORTERROR-STRICT | Codex/codex-review9-parity-importerror-20260708 | 2026-07-08T20:12:16Z | COMPLETE | Already fixed by e00d69a98d; review ledger landed in 6b0997a666; reverified tests/tools/test_parity_gate.py -q on rebased tree (5 passed in 2.58s). |
| REVIEW-18-WASM-DATA-ALIGNMENT | Codex/codex-review18-wasm-data-align-20260708 | 2026-07-08T20:17:10Z | CLAIMED | Fix review finding #18: align each WASM data segment start to its own payload alignment before placement; prove emitted data offsets and wasm backend tests. |
| E1-SCIPY-NI-DOCSTRINGS | Codex/codex-e1-scipy-ni-docstrings-20260708 | 2026-07-08T20:22:08Z | COMPLETE | Landed 689e434d2c: module_attr provider support source is derived from upstream source/build+seal path; real SciPy _nd_image gaussian_filter seal-preflight admits _filters.py, _ni_docstrings.py, _ni_support.py and no scipy/ndimage/__init__.py; py_compile, ruff, fail_closed_gate, 11 focused pytest passed. No cpython-abi lane touched. |
| REVIEW-18-WASM-DATA-ALIGNMENT | Codex/codex-review18-wasm-data-align-20260708 | 2026-07-08T20:29:57Z | COMPLETE | Landed ddb3190ab5. Fixed current-segment data alignment in WasmDataSegments and added allocator + emitted-WASM teeth. Proofs: cargo test -p molt-backend-wasm --features test-util wasm_data::tests (2 passed); cargo test -p molt-backend --features wasm-backend --test wasm_data_segments (9 passed). |
| REVIEW-13-SPECIALIZED-LIST-GIL-FASTPATH | Codex/codex-review13-specialized-list-gil-20260708 | 2026-07-08T20:59:43Z | CLAIMED | Fix review finding #13 on live authority: remove with_gil_entry_nopanic from primitive-only specialized/generic list fast paths and keep GIL only for heap-ref inc/dec or slow promotion. |
| E4-FAILCLOSED-TINYGRAD-QUARANTINE | Codex/codex-e4-tinygrad-quarantine-20260708 | 2026-07-08T21:00:51Z | CLAIMED | Harden fail_closed_gate so production paths cannot wire the quarantined tinygrad reference through tests.helpers.tinygrad_stdlib_loader/TINYGRAD_STDLIB; tests may still load only through the helper. |
| E4-FAILCLOSED-TINYGRAD-QUARANTINE | Codex/codex-e4-tinygrad-quarantine-20260708 | 2026-07-08T21:10:19Z | COMPLETE | Landed ffd254f6cd: fail_closed_gate now rejects production references to tests.helpers.tinygrad_stdlib_loader/TINYGRAD_STDLIB/tinygrad_stdlib_context while allowing tests to use the sanctioned helper; py_compile, ruff, test_fail_closed_gate.py -q (20 passed), fail_closed_gate.py OK. |
| REVIEW-13-SPECIALIZED-LIST-GIL-FASTPATH | Codex/codex-review13-specialized-list-gil-20260708 | 2026-07-08T21:22:13Z | COMPLETE | Landed 23aca6b715. Regular STORE_SUBSCR_LIST_INT, raw-index list store, and unchecked list getitem now bypass with_gil_entry_nopanic for inline primitive values and enter the canonical refcount path only when heap refs change. Proof after rebase: cargo test -p molt-runtime specialized_list --lib (2 passed, 484 filtered out). |
| REVIEW-10-CI-GATE-REQUIRED-SKIP-FAILCLOSED | Codex/codex-review10-25-ci-gate-skip-20260708 | 2026-07-08T21:27:03Z | CLAIMED | Close review #10 on live ci_gate authority: required missing prerequisites must be unmet-prerequisite and exit nonzero; inspect sibling #25 before landing so skip-script detection stays unified. |
| REVIEW-25-CI-GATE-SCRIPT-SKIP-GUARD | Codex/codex-review10-25-ci-gate-skip-20260708 | 2026-07-08T21:27:18Z | CLAIMED | Close review #25 with #10: ci_gate must scan command argv for tools/*.py, fail closed for required missing scripts, and keep optional skips distinct. |
| REVIEW-10-CI-GATE-REQUIRED-SKIP-FAILCLOSED | Codex/codex-review10-25-ci-gate-skip-20260708 | 2026-07-08T21:39:35Z | COMPLETE | Landed bb4881d506. ci_gate required missing prerequisites are unmet-prerequisite; JSON success/summary and required-failure exit include unmet-prerequisite; optional missing prereqs remain skip. Proof: uv run --active --project . --python 3.12 pytest tests/tools/test_ci_gate.py tests/test_dx_run_context.py -q (59 passed). |
| REVIEW-25-CI-GATE-SCRIPT-SKIP-GUARD | Codex/codex-review10-25-ci-gate-skip-20260708 | 2026-07-08T21:40:29Z | COMPLETE | Landed bb4881d506. ci_gate now scans command argv for tools/*.py before execution; required missing uv-run tool scripts become unmet-prerequisite and exit nonzero instead of a false skip. Proof: uv run --active --project . --python 3.12 pytest tests/tools/test_ci_gate.py tests/test_dx_run_context.py -q (59 passed). |
| E4-RUNTIME-CARGO-LOCK-GENERATED-AUTHORITY | Codex/codex-runtime-cargo-lock-authority-20260708 | 2026-07-08T21:44:13Z | CLAIMED | Generated lockfile authority follow-up for runtime satellite-crate decomposition: runtime/Cargo.toml already admits new crates, runtime/Cargo.lock must be regenerated and locked. |
| E4-RUNTIME-CARGO-LOCK-GENERATED-AUTHORITY | Codex/codex-runtime-cargo-lock-authority-20260708 | 2026-07-08T21:45:35Z | COMPLETE | Landed 9c8cc978fd. runtime/Cargo.lock now matches the already-landed runtime satellite crates in runtime/Cargo.toml, including audit/constants/diagnostics/platform/resource and graphlib ordering. Proof: cargo metadata --locked --manifest-path runtime/Cargo.toml --format-version 1 --no-deps; git diff --check clean. |
| REVIEW-23-RUNTIME-WASM-CACHE-PUBLISH-TELEMETRY | Codex/codex-review23-runtime-wasm-cache-telemetry-20260708 | 2026-07-08T21:46:51Z | CLAIMED | Close review #23: runtime_wasm_cache shared publish failures must be counted and surfaced in build diagnostics instead of silently causing cold-every-session. |
| REVIEW-23-RUNTIME-WASM-CACHE-PUBLISH-TELEMETRY | Codex/codex-review23-runtime-wasm-cache-telemetry-20260708 | 2026-07-08T21:51:44Z | COMPLETE | Landed 631c2ab5f2. Verified current authority already fixes #23: runtime_wasm_cache counts hydrate/publish attempts, failures, last publish error; runtime_build warns on human output; build_diagnostics emits runtime_wasm_cache summary/payload. Proof: uv run --active --project . --python 3.12 pytest tests/cli/test_cli_runtime_wasm_shared_cache.py plus four runtime-wasm-cache diagnostics tests in tests/cli/test_cli_import_collection.py -q (15 passed). |
| REVIEW-14-SPECIALIZED-LIST-SLICE-FLAT-BUILDER | Codex/codex-review14-specialized-list-slice-20260708 | 2026-07-08T22:19:44Z | CLAIMED | Close review #14: specialized-list slicing must allocate destination flat storage directly, preserve flat specialization, and avoid temp Vec then copy. |
| REVIEW-14-SPECIALIZED-LIST-SLICE-FLAT-BUILDER | Codex/codex-review14-specialized-list-slice-20260708 | 2026-07-08T22:40:16Z | COMPLETE | Landed 9a9b840146. object::builders now owns flat specialized-list allocation; list[int]/list[bool] slice, copy, and repeat preserve flat storage without temp boxed Vec. Proof 20260708T223806-review14-specialized-list-slice-postrebase-22552c4d5b934b6a passed molt-runtime --lib (473 passed, 16 ignored). |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-08T22:40:27Z | CLAIMED | Claim build-wallclock lane; aperture is dx_build_timer/profile evidence authority for cold-vs-warm build latency, then remove structural causes of serial/cold rebuilds without touching E1/cpython-abi/wasm reserved-callable lanes. |
| REVIEW-16-SCCP-FLOAT-REPR-PARITY | Codex/codex-review16-sccp-float-repr-20260708 | 2026-07-08T22:48:39Z | CLAIMED | Close review #16: SCCP str/repr concrete folding must match CPython for float formatting and repr(str) quote/escape selection; first step inspect eval authority and existing SCCP teeth. |
| REVIEW-16-SCCP-FLOAT-REPR-PARITY | Codex/codex-review16-sccp-float-repr-20260708 | 2026-07-08T22:58:46Z | COMPLETE | Landed af7fe19820 and ledger a873b107c6. SCCP eval folds float str repr only in the finite non-scientific CPython-matching regime and defers exponent nonfinite plus repr(str) quote/escape cases to runtime. Proof run 20260708T225511-review16-molt-passes-lib-84f936bef4a14683 passed molt-passes --lib 841 passed. Adversarial checks: CPython 3.12 and Rust formatting probes confirmed thresholds; rg found no second SCCP fold authority. |
| REVIEW-16-SCCP-FLOAT-REPR-PARITY | Codex/codex-review16-sccp-float-repr-corrective-20260708 | 2026-07-08T23:00:26Z | CLAIMED | REOPEN after adversarial review found finite non-scientific f64 mismatch: current SCCP fold uses Rust Display for 0x4289368ec8725340 -> wrong CPython string. Fix now: delete unsafe float folding or route through a real shared CPython formatter; add teeth. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-08T23:05:09Z | PROGRESS | Landed 6329ea89ca dx: preserve warm target for generated sessions. Generated RunContext custody ids now carry MOLT_SESSION_ID_GENERATED=1 and no longer force per-session Cargo targets on nested DX re-entry; ordinary builds stay on C:\Molt\target, explicit --session-id remains isolated. Proof: pytest tests/test_dx_run_context.py tests/tools/test_throughput_env.py tests/tools/test_dev_py.py -q = 62 passed; py_compile src/molt/dx.py tools/run_context_env.py; drift gate passed. Baseline dx_build_timer row 20260708T224401-e2-build-wallclock-baseline-bb77d82a06-82dd5945f7ef4912 failed stale with running-proof-child-missing then memory-guard-summary-incomplete, so next blocker is proof_queue/memory_guard custody for reliable build-wallclock profiling. |
| REVIEW-16-SCCP-FLOAT-REPR-PARITY | Codex/codex-review16-sccp-float-repr-corrective-20260708 | 2026-07-08T23:09:27Z | COMPLETE | Corrective landed 78b3cf8d17 after adversarial refutation of af7fe19820. Deleted unsafe SCCP fold_float_str/Rust Display path; str(float)/repr(float) now always defer to runtime CPython formatter until it is extracted below molt-passes. Added exact f64 0x4289368ec8725340 eval and pass-level teeth; CPython 3.12.13 gives 3465264303690.4062 while Rust Display gives 3465264303690.4063. Rebased proof 20260708T230715-review16-float-repr-corrective-rebased-molt-passes-lib-20c8382bbd214c70 passed cargo test -p molt-passes --lib: 842 passed; rustfmt changed check clean; no duplicate SCCP fold authority found. |
| E1-CYTHON-CIMPORT-CUSTODY | Codex/codex-e1-cython-cimport-custody-20260708 | 2026-07-08T23:13:38Z | CLAIMED | Source-derived Cython cimport custody: resolve .pxd/header roots from package/source custody for sibling upstream packages; no package-specific overlays or Cython version pin. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-08T23:15:14Z | PROGRESS | Landed dd02b47a31 proof-queue: keep live Windows child-runner rows nonterminal. Windows memory-guard child runner loss is an infra diagnostic while the queue-owned guard is live, not terminal stale. Proof: py_compile tools/proof_queue.py; pytest tests/tools/test_proof_queue.py -q = 157 passed; tree_drift_check --fetch OK. Resuming dx_build_timer baseline. |
| E1-CYTHON-CIMPORT-CUSTODY | Codex/codex-e1-cython-cimport-custody-20260708 | 2026-07-08T23:34:23Z | COMPLETE | Landed 096f686f38: source-derived Cython cimport roots from source-plan package roots plus MOLT_MODULE_ROOTS; generic pxd/header roots, no package overlays or pins. Proof: worktree pytest source_extension 11 passed 2 skipped; canonical real SciPy _ni_label 12 passed; py_compile; ruff; fail_closed_gate OK. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-08T23:38:42Z | PROGRESS | Landed 7c2caf49fc dx: scope backend daemon builds to --bin molt-backend after preserving c6f049a2e6 dx_build_timer preflight authority. Removes typed_repr_report diagnostic bin from backend daemon materialization/timing path and records timer bin metadata. Proof: py_compile backend_binary.py dx_build_timer.py; pytest tests/tools/test_dx_build_timer.py tests/cli/test_cli_backend_binary_materialization.py and two backend-binary import-collection tests = 11 passed on rebased main. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-capsule-dispatch-20260708 | 2026-07-08T23:52:55Z | PROGRESS | CAPSULE FRONTIER (note 103) RESOLVED + LANDED. Root cause was NOT PyCapsule_New symbol resolution: it was a WASM table-index misdispatch in the split-runtime host shims. remapLegacyRuntimeSharedTableIndex hijacked a real app-local funcref at raw call_indirect index 303 (=legacyBase256+reservedBase33+14) into the reserved region offset 14=molt_types_capsule_new; a genuine 4-arg indirect call f("__class__","_",None,None) during numpy import was dispatched to the capsule type __new__, raising the spurious TypeError: cannot create capsule instances. Proven via app.wasm call-indirect trace (arg reprs are strings, not a capsule+ptr). molt_types_capsule_new refusal is correct CPython behavior and UNCHANGED. Fix (class-level, occupancy gate): remap now takes rawIndexHasInstalledEntry; an index resolving to an installed funcref is a genuine indirect call, returned unchanged; bare (empty) legacy references still relocate. Wired into run_wasm.js + browser_host.js + browser_embed.js (cross-platform parity). Regression test added (test_wasm_runner_table_base.py: preserves-installed-app-slot + relocates-bare-reference, both PASS). Landed dd4afb3568 on origin/main. E2E fresh queue build RUN_ID 20260708T232531-pact-witness-acceptance-899bda78320a479f: capsule TypeError GONE (0 occ), build+link+split OK. NEW distinct blocker (numpy-custody seal completeness, M58): ImportError cannot import name version from numpy at numpy/__init__.py:93 (from . import version). numpy/version.py is a build-generated module absent from BOTH the sealed numpy/ dir AND the molt-built numpy-2.4.2 whl. Correct next fix: seal/closure generation must provide numpy/version.py (pinned version string) — do NOT loosen the import (fail-closed is correct). |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-09T00:00:15Z | PROGRESS | Landed 1a1db6dd3c memory-guard: bound cargo quarantine to owned target. Prior proof row 20260708T234323-e2-build-wallclock-bin-lib-2148c02def-ae9ffc7c48db437a passed but its receipt C:\Molt\target\.molt_state\quarantine\cargo_incremental\20260708-234819-pid37920-orphaned_processes_cleaned\receipt.json showed target-root quarantine moved sibling target\sessions\* and nested prior quarantine incrementals, recreating cold-build churn. Scanner now enumerates only direct profile/triple incremental dirs for the passed target; explicit target\sessions\<id> still quarantines itself. Proof: py_compile memory_guard/cargo_quarantine; pytest tests\test_memory_guard_tool.py -q = 114 passed, 1 skipped. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-09T00:07:47Z | PROGRESS | Landed 988c53755a memory-guard: terminalize worker summary gaps. Windows memory_guard wrapper now writes a terminal worker-exit summary when the internal guard worker exits before final JSON; proof_queue classifies memory-guard-worker-exit-without-final-summary instead of falling to stale running JSON/unclassified failure. Proof: py_compile memory_guard.py proof_queue.py diagnostics.py; pytest tests/tools/test_memory_guard_windows_sampling.py tests/test_memory_guard_tool.py tests/tools/test_proof_queue.py -q = 308 passed, 1 skipped on rebased main. Prior wallclock row 20260708T234036 produced partial timings (prime 241.52s, inc-value_range 136.02s, both --bin molt-backend) then hit the fixed lifecycle gap during inc-function_compiler. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-09T00:14:30Z | PROGRESS | Fixed-queue wallclock rerun PASSED: RUN_ID 20260709T000837-e2-build-wallclock-fixed-queue-a5b89b847c-09ab7c0b0aef4e27, JSON logs/proof_queue/e2_build_wallclock_fixed_queue_a5b89b847c.json. Measurements with cargo build -p molt-backend --bin molt-backend --features native-backend: prime 93.91s rc=0; inc-value_range 87.89s rc=0; inc-function_compiler 58.62s rc=0. Delta vs stale pre-bin baseline prime 122.00s: -28.09s (-23.0%) with stale touch path eliminated. Delta vs prior fixed-bin partial same target: prime 241.52s -> 93.91s (-147.61s, -61.1%); inc-value_range 136.02s -> 87.89s (-48.13s, -35.4%). Remaining crux: pass edit still costs O(backend daemon cone), so next structural move is deleting the production molt-tir -> molt-passes dependency / moving shared backend-visible facts below passes. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-09T00:24:30Z | PROGRESS | Landed e61ec66a34 tools: repair dx timer source-touch probes. Cargo -vv row 20260709T000935 showed repeated warm primes were self-inflicted by dx_build_timer restoring value_range/mod.rs after a touch while leaving touched-content artifacts; next prime rebuilt molt-passes and the dependent chain. Timer now repairs after each restored synthetic touch and records repair_samples_sec/repair_rc. Proof: py_compile tools/dx_build_timer.py; pytest tests/tools/test_dx_build_timer.py -q = 9 passed. Queue proof 20260709T001733 passed: prime 81.58s, test-lib 48.58s, repair 74.05s, no touch journal. Immediate no-touch proof 20260709T002311 passed: warm prime 1.69s, warm build 1.49s, no diagnostics. Next blocker moved to real edit rebuild chain cost for value_range/molt-passes -> molt-tir -> backend-native/backend ThinLTO, not stale target custody. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-numpy-version-seal-20260709 | 2026-07-09T00:42:00Z | PROGRESS | NUMPY-SEAL VERSION FRONTIER (note 145) RESOLVED + LANDED as 6dabfa7db1 (coordinator cherry-picked my 6e1d3d2ac1 onto latest main, drift gate PASS, regression test independently reproduced 5/5). Root cause: numpy/version.py AND numpy/__config__.py are meson build-generated modules absent from numpy's source tree, so the source-derived support-file closure silently dropped them (a package-relative import whose candidate does not resolve on disk is treated as "not a module"). numpy/__init__.py imports both unconditionally (L93 `from . import version`; L112 `from numpy.__config__ import show_config`, which fails closed on ModuleNotFoundError), and numpy/_core/__init__.py L11 `from numpy.version import version`. Class-level fix (NOT a one-off copy): new tools/pact_witness_numpy_generated_modules.py re-derives both modules from numpy's OWN authorities — version.py via numpy's committed _build_utils/gitversion.py (version "2.4.2" sourced from numpy pyproject.toml, git_revision from checkout HEAD; never a hand-typed literal, matches wheel dist-info METADATA), __config__.py copied from the real meson-generated build output (fails closed rather than fabricate compiler metadata) — and materializes them into every witness numpy module root (sealed root + off-the-shelf). Wired into tools/pact_seal_witness_roots.py so a re-seal re-materializes them (can never be silently wiped). Regression test tests/tools/test_pact_witness_numpy_generated_modules.py (5 cases: both modules into every root w/ authoritative pyproject version; --check flags missing then passes; missing __config__ build output + version mismatch fail closed). E2E RUN_ID 20260709T001728-pact-witness-acceptance-68118bce2e454495: "cannot import name 'version'" GONE (0 occ), build executes well past numpy/__init__.py:93. NEW distinct blocker (NOT this lane, routed to capsule-dispatch agent): TypeError: cannot create 'capsule' instances deeper in numpy init — a second split-runtime capsule table-dispatch case past the version import. E1 witness-closure completeness advanced; standing down. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-09T00:35:45Z | PROGRESS | Landed 0361d24bc2 memory-guard: keep successful cargo proof caches. Successful cargo-shaped guard exits with post-exit orphan cleanup no longer trigger Cargo incremental quarantine; failed cargo + orphan cleanup still quarantines. Proof: py_compile memory_guard/cargo_quarantine; pytest tests/test_memory_guard_tool.py -q = 116 passed, 1 skipped; queue run 20260709T003126-e2-build-wallclock-cargo-success-no-quarantine-e93e25da-f322e587ca2f4b2f PASSED rc=0 elapsed=9.9s with cargo_incremental_quarantine=null, no orphaned_process_groups, no new quarantine receipt. Prior bad receipt 20260709-001125-pid16488 moved C:\Molt\target\release-fast\incremental after rc=0; this class is closed. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-09T00:53:22Z | PROGRESS | Landed 7499903c47 dx: drop release-fast daemon lto. release-fast is now explicitly iteration-only with lto=off, while shipped release-output and wasm-release retain fat LTO; manifest contract teeth were refreshed to current molt-backend-native split. Proofs: tests/cli/test_backend_manifest_contract.py -q = 26 passed; queue probe 20260709T004300 with CARGO_PROFILE_RELEASE_FAST_LTO=off showed inc-value_range 8.42s; actual patched-profile queue run 20260709T004924 passed on shared C:\Molt\target with prime 119.98s, inc-value_range 7.42s, repair 6.27s, cargo_incremental_quarantine=null. Delta vs ThinLTO evidence 20260709T000837 inc-value_range 87.89s: -80.47s (-91.6%). Fresh-eyes adversarial review agreed the profile move is sound only with patched-profile proof, now satisfied. Next blocker moved back to the real structural class: production molt-tir -> molt-passes backend-visible fact coupling, not LTO/link tax. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-09T01:06:46Z | PROGRESS | Split dx_build_timer daemon/test profile authority. Post-LTO row 20260709T005742 showed normal daemon edit cones are now small (prime 1.52s, inc-function_compiler 5.53s, inc-modules 1.92s) but the harness was still compiling optimized release-fast lib tests after a pass touch: test-lib 106.30s. tools/dx_build_timer.py now keeps daemon scenarios on release-fast, adds --test-profile default dev-fast for test-lib, and records profile/test_profile in JSON; docs updated. Proofs: py_compile dx_build_timer; tests/tools/test_dx_build_timer.py -q = 9 passed; queue row 20260709T010523 passed with meta profile=release-fast/test_profile=dev-fast, prime 10.31s, test-lib 6.52s, repair 7.09s. Delta vs the measured optimized-test path: -99.78s (-93.9%) on the timer's proof-compile scenario. Next blocker remains the deeper crate-boundary class: broad production molt-tir -> molt-passes public/pass-fact coupling for non-backend-visible pass edits; measure inc-gvn next before cutting. |
| E1-SCIPY-NI-SUPPORT | Codex/codex-e1-scipy-ni-support-20260708 | 2026-07-09T04:24:59Z | COMPLETE | Queued proof RUN_ID 20260709T042311-20260709t0135-e1-scipy-ni-support-scripted-build-6f2d4342726243a1 passed rc=0: real SciPy _nd_image wasm source-plan build produced object_count=8, linked_object_count=8, errors=[], includes upstream src/ni_support.c as 7_ni_support.o and provider-derived support scipy/ndimage/_ni_support.py via gaussian_filter/_filters. fail_closed_gate OK; no package overlay or cpython-abi/witness lane touched. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-capsule-dispatch-20260708 | 2026-07-09T04:43:10Z | PROGRESS | CAPSULE FRONTIER RE-VERIFIED CLOSED (no new fix needed). Coordinator flagged a "new capsule blocker" (TypeError cannot create capsule instances, RUN 20260709T001728) as deeper than my occupancy-gate fix (dd4afb3568). Investigated: that run used the SHARED-checkout run_wasm.js at an OLDER commit (runDirectLink@5551 vs current @5561) that PREDATED the occupancy fix landing there. Replaying 001728 app.wasm with the fixed host + an AUTHORITATIVE fresh queue build off latest main (RUN_ID 20260709T042459-pact-witness-acceptance-cc88f2a5d4e24b13) BOTH show 0 capsule reserved-dispatches (verified via MOLT_CAPSULE_STACK_DEBUG probe) and advance PAST capsule. The 001728 capsule was the SAME pre-fix table-index misdispatch, masked at the numpy __init__.py:94 import point; the occupancy gate is robust (no different-window / bare-vs-installed edge miss). REAL current blocker (numpy-custody, M56/M58 — NOT dispatch): ModuleNotFoundError No module named numpy._expired_attrs_2_0 at numpy/__init__.py:94 (from ._expired_attrs_2_0 import __expired_attributes__). Root: the witness seal numpy/ is a PARTIAL package (only __init__.py, _core, + coordinator-materialized version.py/__config__.py); pure-Python SOURCE submodules that live only in bench/friends/repos/numpy_off_the_shelf/numpy/ (_expired_attrs_2_0.py present there, next _globals.py line 95, then lib/ matrixlib/ line 457, ...) are NOT staged into the closure, so they fail closed at runtime. Correct next fix (extends 6dabfa7db1 pattern): the witness numpy closure/seal staging must resolve+stage numpy pure-Python submodules across ALL module roots (numpy_off_the_shelf), not just the partial seal dir — a systematic closure-completeness fix, not per-file materialization. Handing this to the numpy-custody lane; capsule/dispatch frontier is DONE. |
| E4-FAILCLOSED-ECOSYSTEM-OVERLAY-GUARD | Codex/codex-e4-ecosystem-overlay-guard-20260709 | 2026-07-09T05:05:30Z | CLAIMED | Harden Scan F so new package-specific regen/config/header/source-plan overlays for NumPy/SciPy/pandas/tinygrad fail closed; no package-specific support path added. |
| E4-FAILCLOSED-ECOSYSTEM-OVERLAY-GUARD | Codex/codex-e4-ecosystem-overlay-guard-20260709 | 2026-07-09T05:13:50Z | COMPLETE | Landed 9a15ba2622: Scan F now rejects new package-specific source-plan/config/header overlays for NumPy/SciPy/pandas/tinygrad while allowing generic custody tools; no registry baseline bump. Proof: py_compile gate/tests; ruff gate/tests; pytest tests/cli/test_fail_closed_gate.py -q = 20 passed; fail_closed_gate.py OK. |
| REVIEW-17-RAW-DIVIDE-OVERFLOW | Codex/codex-review17-raw-divide-overflow-20260709 | 2026-07-09T05:19:11Z | CLAIMED | Close review finding #17 on current main: raw INT_MIN/-1 divide/mod class appears fixed by 639c9b8974; reverify native/backend and differential teeth, then land ledger only if proof holds. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-09T05:18:50Z | PROGRESS | Landed 0400a8698e dx: add first-class inc-gvn pass-edit timing probe to dx_build_timer defaults, tests, and tool registry so the board's non-value_range pass-edit aperture is measured by the canonical harness rather than ad hoc touches. Queue RUN_ID 20260709T051055-e2-build-wallclock-inc-gvn-before-20260709001054-63badcd96f2e4c31 passed on shared C:\Molt\target: prime 97.41s, inc-gvn 28.92s, repair 14.92s, all cargo build --profile release-fast -p molt-backend --bin molt-backend --features native-backend. Proofs: py_compile tools/dx_build_timer.py; pytest tests/tools/test_dx_build_timer.py -q = 9 passed; drift_harvest --gate passed. Next cut: move backend-visible facts/descriptors below passes or orchestration above backend leaf crates; do not collapse pass bodies into molt-tir because that preserves the pass-edit rebuild cone. |
| REVIEW-17-RAW-DIVIDE-OVERFLOW | Codex/codex-review17-raw-divide-overflow-20260709 | 2026-07-09T05:24:34Z | COMPLETE | Already fixed by 639c9b8974: runtime mod INT_MIN%-1 routes to bigint path; native raw sdiv/srem lanes have debug trap guard + safety attestations and differential int_divmod_i64_boundary.py. Reverified current origin/main has no later changes to the five fixed code/test paths (git log and diff empty) and anchors present. Original proof on 639c: native-backend --lib 138 passed and differential boundary PASS; fresh cargo rerun was not submitted because active Pact witness owns compiler-build-resource. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-scipy-ni-label-graft-20260709 | 2026-07-09T06:05:00Z | PROGRESS | SCIPY `_ni_label` WITNESS GRAFT VERIFIED + GATED. Task was to graft the built scipy.ndimage `_ni_label` C-ext into the witness scipy closure so `scipy.ndimage.label`'s module-level `from . import _ni_label` (scipy/ndimage/_measurements.py:33, calls `_ni_label._label` / catches `_ni_label.NeedMoreBits`) resolves. FINDING (verified, not assumed): prior lane E1-SCIPY-NI-SUPPORT (154) already did the graft durably — `_ni_label.molt.wasm` is staged in its own witness root `tmp/pact_scipy_ni_label_molt_ext_wasm_cpython_abi` at CURRENT ABI (molt_abi4, molt_c_api_version 4), symbol closure SATISFIED (source_c_api_scan missing_symbols=[], fail_fast_symbols=[]; init PyInit__ni_label; required capsule numpy.core._multiarray_umath._ARRAY_API), and the root is REGISTERED in both `tools/proof_queue.py::_pact_witness_native_roots` (→ MOLT_MODULE_ROOTS) and `tools/pact_seal_witness_roots.py::_DEFAULT_WITNESS_ROOTS`. The external-native resolver UNIONS native artifacts across roots per package, so `_ni_label` (sibling root) presents as `scipy.ndimage._ni_label` alongside `_nd_image` (seal_next) under one scipy package — NO physical move into seal_next needed (and none made: moving it would risk clobbering the e1-witness-scipy-pyclosure lane's seal_next .py staging for zero functional gain). `_label`/`NeedMoreBits` are PyInit-runtime module attributes (not ELF/callable exports) — correct; the frontend requirement is that the module import resolves, which it does. VERIFIED via the REAL resolver `_resolve_external_package_native_artifact_plan` over the live witness roots: resolves scipy.ndimage._nd_image (PyInit__nd_image) + scipy.ndimage._ni_label (PyInit__ni_label), errors=[]. Durability: `pact_seal_witness_roots.py --check --root tmp/pact_scipy_ni_label_molt_ext_wasm_cpython_abi` = OK (all manifests at molt_abi4). LANDED: the ungated gap was the resolver-UNION path (root registration was gated by test_proof_queue admits_staged_native_roots, but nothing gated that the resolver actually surfaces scipy.ndimage._ni_label across roots). New regression gate tests/cli/test_external_native_scipy_ni_label_union.py (2 cases: positive union of _ni_label+_nd_image across two roots with real minimal WASM artifacts + valid manifests through the real resolver; negative teeth = dropping the _ni_label root removes scipy.ndimage._ni_label from the closure). Proofs (worktree off origin/main 724137ba7e): pytest new file 2 passed; adjacent test_proof_queue admits_staged_native_roots 1 passed; ruff clean; py_compile OK; fail_closed_gate OK (baseline unchanged); check_table_drift --check PASS. NEXT SUB-BLOCKER (NOT this lane): full scipy.ndimage import still needs the pure-Python scipy/ndimage/*.py closure (e1-witness-scipy-pyclosure lane) staged into the seal; consolidated pact-witness-acceptance E2E confirms once that lands. `_ni_label` native custody is DONE. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-numpy-pyclosure-20260709 | 2026-07-09T07:05:00Z | PROGRESS | NUMPY PURE-PYTHON CLOSURE FRONTIER (note 155 handoff) RESOLVED + LANDED as e031e4b02b (coordinator cherry-picked my 206780eea8 onto latest main, drift gate PASS). Root cause: numpy is admitted as an external static package; the build resolves numpy's pure-Python support surface as the import-driven closure of the SEALED root (package_source_root = tmp/pact_numpy_multiarray_sealed_for_witness), and the off-the-shelf numpy checkout — though a module root — is NOT consulted for the external static package's support files (it owns no native artifact for numpy; confirmed via _resolve_external_package_native_artifact_plan: package_dir is the seal). The seal was staged with only 4 py files (__init__.py, _core/__init__.py, + my version.py/__config__.py), so every pure-Python submodule numpy/__init__.py imports (L94 _expired_attrs_2_0, L95 _globals, L457 lib/matrixlib, + transitive) was dropped from the closure BFS (candidate absent from package_source_root → not a module) and fail-closed at runtime with ModuleNotFoundError. Verified pre-fix: the last run staged exactly 4 numpy .py; even numpy._globals (declared in the manifest runtime_python_import_modules) was dropped because absent from the seal. Systematic one-pass fix (NOT per-submodule whack-a-mole, no loosened import): new tools/pact_witness_numpy_python_closure.py mirrors numpy's ENTIRE importable pure-Python subtree (tests/ excluded; 263 modules) from bench/friends/repos/numpy_off_the_shelf into every witness sealed root, so the EXISTING import-driven support-file closure BFS resolves + stages numpy's full transitive Python import closure; layers the build-generated version.py/__config__.py (pact_witness_numpy_generated_modules, numpy's own authorities) on top; never clobbers the sealed .molt.wasm/manifests. Wired into tools/pact_seal_witness_roots.py so a re-seal re-stages the full closure, with --check. Regression test tests/tools/test_pact_witness_numpy_python_closure.py (6 cases: full subtree mirrors in; tests/ excluded; C-ext .molt.wasm preserved; generated modules not clobbered by source mirror; --check flags partial package + source drift; missing numpy source fails closed) — 6 new + 5 generated-module = 11 passed, ruff clean. E2E RUN_ID 20260709T045445-pact-witness-acceptance-d27e9731005a47f1 (coordinator-verified): "cannot import name version" AND "_expired_attrs_2_0" ModuleNotFoundError both GONE (0 occ) — numpy's full pure-Python subtree now imports. NEW distinct blocker (NOT this lane, routed to runtime/cpython-abi agent): ImportError Importing the numpy C-extensions failed — _multiarray_umath static-link PyModuleDef Py_mod_exec slot returned non-zero: cannot get sys.flags (numpy's C-ext multi-phase init reads sys.flags, unprovided by molt runtime). E1 numpy pure-Python closure completeness DONE; standing down. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-build-wallclock-20260708 | 2026-07-09T05:40:30Z | PROGRESS | Landed c55d34509d tir: move target info below pass churn. Moved backend-visible target/profile cost-model descriptor authority from runtime/molt-passes/src/tir/target_info.rs to runtime/molt-ir/src/tir/target_info.rs, re-exported through molt-passes and molt-tir, updated crate graph/docs/spec/board, and fixed build_graph_audit generated artifacts to write LF on Windows. Proofs on current origin/main: queue 20260709T053730-e2-target-info-current-check-70e8fc7c23554c4d passed cargo check -p molt-ir -p molt-passes -p molt-tir --all-features; queue 20260709T053750-e2-target-info-current-molt-ir-tests-b95f3af38ca645f5 passed cargo test -p molt-ir --lib --all-features; build_graph_audit --check OK; stale old target_info path scan no matches; only molt-ir declares pub mod target_info; git diff --check OK; drift_harvest --gate passed before ff_land. Next structural cuts: move representation/value-range/backend-visible fact descriptors below pass churn or lift pass orchestration out of backend leaf rebuild cones; do not move pass bodies into molt-tir. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-scipy-pyclosure-20260709 | 2026-07-09T05:52:00Z | PROGRESS | SCIPY PURE-PYTHON CLOSURE PRE-STAGED + LANDED (the scipy analogue of the numpy closure e031e4b02b, pre-staging the NEXT frontier in parallel with the numpy _multiarray_umath fix). Root cause (identical class to numpy): scipy is admitted as an external static package; its pure-Python support surface resolves as the import-driven closure of the SEALED roots (tmp/pact_scipy_ndimage_sealed_for_witness_next + siblings), which carried only scipy/__init__.py + scipy/ndimage/__init__.py, so every submodule scipy/__init__ imports (_distributor_init, _external.packaging_version.version, _lib._testutils, version, __config__, + transitive) was dropped from the closure BFS and fail-closed with ModuleNotFoundError. Systematic one-pass fix (no per-submodule whack-a-mole, no loosened import): NEW tools/pact_witness_scipy_python_closure.py mirrors scipy's ENTIRE importable pure-Python subtree (tests/ excluded, 542 modules) from bench/friends/repos/scipy_off_the_shelf into every witness sealed root; kept a SIBLING of the numpy tool (not refactored into a shared core) because the numpy witness tooling is under concurrent E1 custody -- lower cross-lane blast radius. Handles the one scipy-specific deviation numpy lacks: scipy/_external/packaging_version installs its modules from a src/ subdir (per its own meson install_sources, parsed not hardcoded), so the mirror relocates src/version.py + src/_structures.py up one level so scipy._external.packaging_version.version resolves; package-root generated modules protected by RELPATH not basename so the nested packaging_version/version.py still mirrors. NEW tools/pact_witness_scipy_generated_modules.py materializes scipy/version.py from scipy's OWN tools/gitversion.py --write (1.18.0 from pyproject, revision from HEAD -- never a literal). Wired into pact_seal_witness_roots.py (additive, alongside numpy) with --check. Regression test tests/tools/test_pact_witness_scipy_python_closure.py (8 cases incl. the packaging_version install-rename + __config__ fail-closed-but-pure-Python-persists) -- 8 passed, +14 numpy/scipy closure tests green, fail_closed_gate OK. VERIFIED against the REAL off-the-shelf scipy checkout: closure went 1625 -> 1 missing modules (the 1 remainder = the __config__.py blocker below). Landed 237a6fd488 (scipy closure) + b1edc355a4 (fail_closed_gate: allowlist the witness closure seal-stagers -- fixes a PRE-EXISTING main red where e031e4b02b's numpy closure stager tripped the Scan-F overlay regex on its docstring prose and landed unregistered; correct classification is _BUILD_CRUTCH_ALLOWLIST per its own adapters/seal-verifiers exemption, since the ratchet baseline may only be lowered). E2E RUN_ID 20260709T055144-pact-witness-acceptance-ef44ba149c4b42c1 (detached). TWO precise remaining scipy blockers past this closure (NOT this lane -- deep native sub-frontiers): (1) scipy/__config__.py is NOT on disk anywhere (only __config__.py.in template); it needs a full scipy wasm `meson setup` -- configure_file at scipy/meson.build ~L782 substitutes compiler ids/versions + BLAS/LAPACK/pybind11 dep resolution (e.g. -Dblas=none -Dlapack=none). My tool FAILS CLOSED naming this, refusing to fabricate compiler metadata. (2) scipy._lib._ccallback_c (a Cython ext eagerly imported by scipy/__init__ via _lib._ccallback) has NO sealed wasm artifact yet (only _nd_image + _ni_label are sealed) -- a new native frontier like _multiarray_umath. scipy pure-Python + version.py closure completeness DONE; standing down. |
| REVIEW-15-FRONTEND-FREE-VAR-MEMOIZATION | Codex/codex-review15-free-var-memo-20260709 | 2026-07-09T06:00:00Z | CLAIMED | Close review #15 by memoizing frontend free-variable analysis at the collector authority; prove no scope semantics drift. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-scipy-pyclosure-20260709 | 2026-07-09T06:00:00Z | PROGRESS | E2E VERDICT for run 20260709T055144-pact-witness-acceptance-ef44ba149c4b42c1: FAILED at BUILD (rc=2, 180s) with fail-closed "reachable native support source imports native package modules without source or artifact custody: scipy._lib._ccallback_c (source: scipy/_lib/_ccallback_c.pyx)". This CONFIRMS the scipy pure-Python closure is now EFFECTIVE: reachability now follows scipy/__init__ -> scipy._lib._ccallback (.py, newly staged) -> `from . import _ccallback_c`, so the build correctly discovers the _ccallback_c Cython ext requirement it previously could not see (before staging, _ccallback.py was absent so the chain stopped short and _ccallback_c was never reached -- the prior "build success" that reached the numpy _multiarray_umath RUNTIME frontier was an artifact of scipy being UNDER-staged). Honest consequence: the shared witness build now fails-closed at scipy _ccallback_c custody, which is EARLIER than the numpy _multiarray_umath runtime frontier -- so this E2E path no longer reaches that numpy runtime signal (numpy/runtime lane: exercise the sys.flags fix via a numpy-only witness or unit path). No fake/stub used to dodge it (M05). CONCRETE NEXT E1 NATIVE FRONTIER (build-time, blocks scipy import): build+seal scipy._lib._ccallback_c to wasm32 static-link via source-extension custody (the _nd_image/_ni_label pattern) so scipy/__init__'s eager `from scipy._lib._ccallback import LowLevelCallable` resolves; THEN scipy/__config__.py (meson setup). scipy pyclosure lane work DONE. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-umath-linalg-20260709 | 2026-07-09T01:05:00Z | PROGRESS | numpy.linalg COMPUTE-PATH LONG POLE landed (rebased on origin/main, drift gate PASS). `numpy/linalg/_linalg.py` does a module-level `from numpy.linalg import _umath_linalg`, so the absent C ext blocked numpy IMPORT (not just `np.linalg.eigh`). Compiled numpy `_umath_linalg` (C++ gufuncs + self-contained f2c `lapack_lite` LAPACK/BLAS) to wasm32-wasip1 cpython-abi via the EXISTING numpy Meson build metadata (regen_numpy_multiarray_meson_wasm.py) + `molt extension build`. REAL LAPACK, zero stubs: `dsyevd_`/`ssyevd_`/`cheevd_`/`zheevd_` (eigh drivers) + geev/gesdd/getrf/potrf all defined in the wasm binary (llvm-nm T), 5257 project-defined symbols, missing=0 fail_fast=0. Symbol closure fully resolvable: 45 runtime_symbols = Py-C-API + libm + compiler-rt + 5 npy_* + 2 numpy capsules (_ARRAY_API/_UFUNC_API), all provided at witness link. NEW `molt extension build --source-plan-exclude-linked-static-library`: numpy links libnpymath.a into BOTH _multiarray_umath (PRIMARY) and _umath_linalg -> 168 duplicate global npy_* = fatal `wasm-ld: duplicate symbol` at the single-module witness link (REPRODUCED both -r and final). Excluding npymath makes _umath_linalg thin (npy_* undefined -> resolve from multiarray's exports, the _nd_image shape); VERIFIED sealed _multiarray_umath + _umath_linalg wasm-ld-merge = 0 duplicates, and all 5 externalized npy_* are DEFINED in multiarray. Sealed into tmp/pact_numpy_multiarray_sealed_for_witness/numpy/linalg/ (disjoint from pyclosure); pact_seal_witness_roots.py --check OK at molt_abi4, extension audit OK (static_link/checksum/object_closure/python_export). Durable producer tools/build_numpy_umath_linalg_wasm.py (re-seal reproduces). Regression test test_extension_build_excludes_linked_static_library PASS. Consolidated pact-witness-acceptance E2E will confirm once numpy+scipy pure-Python closures fully land. Pre-existing unrelated red on main: test_extension_build_wasm_target_emits_static_link_artifact_and_manifest (molt_alloc/malloc runtime-ABI symbol-facts drift, fails identically on pristine origin/main). |
| REVIEW-15-FRONTEND-FREE-VAR-MEMOIZATION | Codex/codex-review15-free-var-memo-20260709 | 2026-07-09T06:14:28Z | COMPLETE | Landed d6955e5bec: raw frontend free-var analysis is cached per AST node and list/filter paths project from the single raw authority; generated protocol refreshed. Proof: tests/test_frontend_sema.py 48 passed; tests/test_frontend_package_composition.py 16 passed; gen_protocol --check; py_compile touched files; ruff touched files; fail_closed_gate OK. |
| REVIEW-7-FRONTEND-PHASE-TIMEOUT-PORTABLE | Codex/codex-review7-8-frontend-timeout-20260709 | 2026-07-09T06:17:58Z | CLAIMED | Verify review #7 on current main: portable MOLT_FRONTEND_PHASE_TIMEOUT appears already fixed by 22f6db2a75; will reprove exact worker timeout contract and mark complete if held. |
| REVIEW-8-FRONTEND-PHASE-TIMEOUT-PARALLEL | Codex/codex-review7-8-frontend-timeout-20260709 | 2026-07-09T06:18:27Z | CLAIMED | Verify review #8 with #7: timeout no longer disables frontend parallel config; per-worker timeout path already present on current main. Will reprove and complete ledger. |
| REVIEW-7-FRONTEND-PHASE-TIMEOUT-PORTABLE | Codex/codex-review7-8-frontend-timeout-20260709 | 2026-07-09T06:18:54Z | COMPLETE | Already fixed on current main by 22f6db2a75: _phase_timeout uses POSIX setitimer only where valid and a portable watchdog off-main-thread/Windows, so MOLT_FRONTEND_PHASE_TIMEOUT is configured==effective. Reverified tests/cli/test_cli_frontend_worker_authority.py + tests/cli/test_cli_frontend_scc_scheduling.py -q: 13 passed. |
| REVIEW-8-FRONTEND-PHASE-TIMEOUT-PARALLEL | Codex/codex-review7-8-frontend-timeout-20260709 | 2026-07-09T06:19:13Z | COMPLETE | Already fixed on current main by 522b7fe040 plus current timeout tests: _resolve_frontend_parallel_config no longer takes/disables on frontend_phase_timeout, and workers apply timeout per module. Reverified tests/cli/test_cli_frontend_worker_authority.py + tests/cli/test_cli_frontend_scc_scheduling.py -q: 13 passed. |
| REVIEW-6-WASM-TAIL-CALL-FEATURE-GATE | Codex/codex-review6-wasm-tail-call-gate-20260709 | 2026-07-09T06:20:40Z | CLAIMED | Gate WASM return_call emission through compile options/target capability instead of frame eligibility alone; keep module_abi target_features lane untouched. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-scipy-config-wasm-20260709 | 2026-07-09T06:40:00Z | PROGRESS | SCIPY __config__.py BLOCKER (named in row 164 as remaining scipy blocker #1) CLOSED + LANDED 05287a0669 on origin/main (rebased, SSH, drift gate PASS, fail_closed_gate OK). scipy/__init__.py:48 does `from scipy.__config__ import show` and FAILS CLOSED (re-raises ImportError) when absent; scipy/__config__.py exists NOWHERE in scipy's source tree (only the __config__.py.in template) -- meson emits it via configure_file (scipy/meson.build ~L782). A full scipy wasm `meson setup` (deep-correct route a) is INFEASIBLE here: scipy's top-level meson.build hard-errors on absent git submodules (array_api_compat/array_api_extra/cobyqa/unuran/xsf -- verified MISSING on disk), then requires Fortran + openblas + every heavy subproject (boost_math/qhull_r/xsf/duccfft/pyprima) to configure, ALL before the subdir('scipy') configure_file at the very end of setup. Route taken (b, done rigorously, ZERO fabrication): NEW tools/pact_witness_scipy_config_meson_wasm.py drives meson's OWN configure_file on scipy's OWN __config__.py.in, with configuration_data from a faithful, line-cited mirror of scipy/meson.build v1.18.0 L702-788. EVERY value is meson-RESOLVED from the same wasm cross toolchain scipy would use (molt's wasm32-wasip1 clang/clang++/cython, IDENTICAL cross file to the numpy _multiarray seal): clang 22.1.7/ld.lld, cython 3.2.8, fortran mirroring the C compiler with args=n/a (scipy's real _without-fortran branch), pythran 0.18.1, pybind11 3.0.4 (config-tool), blas/lapack via dependency('none')->name none/found false, host wasm32/wasi, build x86_64/windows, cross-compiled true, python 3.12. These are verifiably IDENTICAL to the real numpy wasm __config__.py for the shared toolchain fields. No hand-typed compiler/BLAS metadata; tool FAILS CLOSED with the exact meson error if setup fails. pybind11+pythran are scipy's OWN declared build deps (pip-installed into the venv, host-independent tool versions). Published to tmp/pact_scipy_config_meson_wasm_build_generated/scipy/__config__.py; pact_witness_scipy_generated_modules.py picks it up as _CONFIG_BUILD_CANDIDATES[0]. VERIFIED: `from scipy.__config__ import show` resolves + show(mode='dicts') returns the real toolchain CONFIG; generated-modules --check OK; scipy python-closure --check now OK (was fail-closed at materialize_config); config materialized into all 4 witness scipy roots. gate stays ecosystem_build_crutch=2<=2 (this producer INVOKES scipy's own build system, not a Molt reimplementation; named pact_witness_* not regen_* to reflect that). REMAINING scipy blocker (NOT this lane): scipy._lib._ccallback_c native wasm ext (row 166, owned by the ccallback native lane). Witness still far from green (candidate_outputs.npz not written); consolidated E2E confirms once _ccallback_c custody clears. scipy __config__ lane DONE; standing down. |
| E2-BUILD-WALLCLOCK | Codex/codex-e2-tir-passes-edge-20260709 | 2026-07-09T06:43:47Z | PROGRESS | Landed d0709c1d19 + bd08d69e28 on origin/main. ValueRangeResult and numeric_facts moved from molt-passes into runtime/molt-ir, re-exported through molt-passes/molt-tir, and pass computation stays in molt-passes; lowering, representation facts, WASM LIR-fast, docs, and crate graph now consume the IR authority. Also repaired current-main WASM split emitter fallout exposed by the proof surface (class_ops/attribute_ops/state_dispatch module wiring and stale helper types). Proofs: 20260709T064146-e2-value-range-final-stack-check-2-b832614c617545fc passed cargo check -p molt-ir -p molt-passes -p molt-tir -p molt-backend-wasm --all-features after final claims rebase; 20260709T063350-e2-value-range-landing-molt-ir-tests-af40532ea4094785 passed molt-ir --lib; 20260709T063413-e2-value-range-landing-molt-passes-tests-0b50e4067ae54a76 passed molt-passes --lib; 20260709T063448-e2-value-range-landing-molt-tir-representation-test-dd4122e4067043f9 passed representation_unification; build_graph_audit --check, git diff --check, tree_drift_check --fetch, drift_harvest --gate passed. Next E2 cut: remove remaining production molt-tir -> molt-passes coupling around pass execution/orchestration/drop attrs without moving pass bodies into backend leaf crates. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-scipy-ccallback-20260709 | 2026-07-09T07:20:00Z | PROGRESS | SCIPY `_ccallback_c` NATIVE FRONTIER (note 166) BUILT+SEALED+GRAFTED, and the 5 blocking CPython C-API symbols it needs IMPLEMENTED in molt-cpython-abi (no stubs, M05). Compiled `scipy._lib._ccallback_c` (scipy's LowLevelCallable machinery, eagerly imported by scipy/__init__ via `_lib._ccallback`) to a wasm32-wasip1 cpython-abi relocatable object via the SAME standalone-Cython -> synthesized source-plan -> `molt extension build` path as `_ni_label` (object_count=1, missing_symbols=[], fail_fast=[], NO numpy capsule -- self-contained; init PyInit__ccallback_c). Import-chain traced: the ONLY new native ext the scipy eager chain (scipy/__init__ -> _lib._ccallback -> `from . import _ccallback_c`) requires is `_ccallback_c`; its cimport `.ccallback` is header-only (ccallback.pxd + _lib/src/ccallback.h), and messagestream/_fpumode/_test_ccallback are NOT eagerly imported (verified by grep of the eager path). FOUR precise blockers found + fixed, each REAL: (1) ccallback.h #includes scipy_config.h (meson configure_file output) -- materialized from scipy's OWN scipy_config.h.in via the standard `#mesondefine X`->`/* #undef X */` rule (wasm single-threaded; byte-identical to the ndimage build's generated copy; package custody, not a Molt overlay). (2-5) the Cython C referenced 5 CPython C-API functions ABSENT from molt-cpython-abi (verified absent in both headers AND src/api, not merely undeclared): `PyLong_AsDouble`, `PyUnicode_IS_ASCII`, `PyUnicode_DecodeASCII`, `PyUnicode_DecodeUTF16`, `PyErr_PrintEx`. Implemented all 5 FAITHFULLY in runtime/molt-cpython-abi (numbers.rs: AsDouble mirrors PyFloat_AsDouble's verified-int read; strings.rs: DecodeASCII validates ASCII then forwards to the UTF-8 allocator like DecodeLatin1, DecodeUTF16 does real String::from_utf16 with byteorder/BOM handling, IS_ASCII via unicode_bytes().is_ascii(); errors.rs: PrintEx delegates to PyErr_Print) + declared in runtime/molt-cpython-abi/include/Python.h. cargo check -p molt-lang-cpython-abi GREEN. Also: `strdup` (libc.string cimport, used by _ccallback_c) was missing from the WASM link-import allowlist -> added `wasm_libc_link_import` to runtime/molt-backend-wasm/src/wasm_abi_manifest.toml + regenerated _wasm_abi_generated.py/.json (gen_wasm_abi.py --check GREEN; WASI libc provides strdup, no runtime impl needed). RESULT: `scipy._lib._ccallback_c` now RESOLVES through the REAL external-native resolver (init PyInit__ccallback_c, no capsule, molt_abi4, errors=[]). Durable producer tools/build_scipy_ccallback_c_wasm.py (reproduces extension_sha256 7ba42631; allowlisted in fail_closed_gate Scan F alongside build_numpy_umath_linalg_wasm.py). Sibling seal root tmp/pact_scipy_ccallback_c_molt_ext_wasm_cpython_abi registered in proof_queue._pact_witness_native_roots + scopes + pact_seal_witness_roots._DEFAULT_WITNESS_ROOTS; pact_seal_witness_roots --root ... = SEALED -> molt_abi4. Proofs: fail_closed_gate OK; gen_wasm_abi --check OK; ruff+py_compile OK; test_proof_queue admits_staged_native_roots extended for the ccallback_c root (PASS); ni_label union test PASS. HONEST STATE: `_ccallback_c` native custody is DONE (build advances PAST the note-166 fail-closed "_ccallback_c has no source or artifact custody" blocker). The 5 C-API impls compile + are ABI-resolvable but full RUNTIME execution is confirmed only by the consolidated witness E2E (RUN_ID below), which is currently gated EARLIER by the SEPARATE scipy/__config__.py meson-frontier blocker (note 166 item 1, another lane) -- scipy/__init__ imports __config__ before _ccallback. Once __config__ lands, the E2E exercises _ccallback_c import + these 5 symbols. E2E queued detached RUN_ID 20260709T064310-pact-witness-acceptance-a75b79d5f7dd48c9 (expected to advance PAST the _ccallback_c build-custody blocker to the separate __config__.py closure blocker). |
| REVIEW-6-WASM-TAIL-CALL-FEATURE-GATE | Codex/codex-review6-wasm-tail-call-gate-20260709 | 2026-07-09T06:51:37Z | COMPLETE | Landed 1eaf960f2e wasm tail-call opcode emission gate. WasmCompileOptions.tail_call_enabled defaults off, flows into direct call emission, and explicit opt-in is proven by diagnostics/opcode count; also repaired reachable wasm_compilation stale contracts (implicit unreachable lowering, class_ops directory-module path, call_async diagnostic expectation). Proof: queue 20260709T064910-review6-wasm-tail-call-gate-latest-9c7f34beeca340b1 passed cargo test -p molt-backend --features wasm-backend --test wasm_compilation (47 passed); rustfmt --changed OK; git diff --check OK; fail_closed_gate OK; tree_drift_check --fetch OK. Did not edit cpython-abi, wasm link/finalize ABI, dynamic call dispatch, or witness seal inputs. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-sysflags-20260709 | 2026-07-09T07:55:00Z | PROGRESS | SYS.FLAGS C-API FRONTIER (note handoff from numpy-pyclosure lane, RUN 20260709T045445 diagnosis `static-pymodexec-nonzero: cannot get sys.flags`) RESOLVED + LANDED as f3b97fa194. Root cause (verified against numpy source, not assumed): numpy `_multiarray_umath` multi-phase init calls `PySys_GetObject("flags")` at `_core/src/multiarray/npy_static_data.c:222` (borrowed) and on NULL sets `AttributeError("cannot get sys.flags")` + returns -1 from its Py_mod_exec slot → whole C-ext import fails closed. molt's `sys` module DOES implement a correct CPython-3.12 `sys.flags` structseq (src/molt/stdlib/sys.py `_SYS_FLAGS_SEQUENCE_FIELDS`: 18 fields debug..int_max_str_digits, values hash_randomization=1, int_max_str_digits=4300, rest 0 — matches CPython 3.12 `Python/sysmodule.c` `flags_fields`, verified against the primary source), BUT it materializes flags (and implementation/version_info/float_info/hash_info/thread_info) into the module dict LAZILY via its PEP 562 module `__getattr__` — the raw sysdict-direct borrowed lookup in `sys_module_attr_borrowed` (runtime/molt-runtime/src/cpython_abi_hooks.rs) bypassed `__getattr__`, so numpy's cold C-API read (which lands before any Python code touches sys.flags) missed. Class-level fix (NOT a per-attr eager hack, no loosened contract): on a raw-dict miss `sys_module_attr_borrowed` now drives the module `__getattr__` (via existing `module_attr_lookup_allow_missing`), which populates the dict as its documented side effect, then re-reads the borrowed handle the dict now owns — preserving `PySys_GetObject`'s CPython borrowed-reference contract and covering ALL lazy sys metadata views uniformly. Regression test `pysys_getobject_missing_attr_on_cold_module_fails_closed` (cold sys dict w/o `__getattr__` stays NULL + no pending-exception leak). Verified: isolated `cargo test -p molt-runtime --lib cpython_abi_hooks::tests::pysys_getobject` = 3 passed (incl. new test) on rebased latest origin/main; Python-side structseq attribute+index coverage already gated by tests/differential/stdlib/sys_flags_intrinsic.py. Full numpy `_multiarray_umath` PySys_GetObject path to be confirmed by pact-witness-acceptance E2E (queued). NEXT: numpy C-ext init advances past sys.flags; downstream blocker TBD from the E2E. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-witness-mem-diagnosis-20260709 | 2026-07-09T08:20:00Z | PROGRESS | MISDIAGNOSIS CORRECTED (evidence, not assumption): the "consolidated build runs OUT OF MEMORY during numpy+scipy frontend lowering" premise for RUN 20260709T071828 (rc=15 at 453s — the sys.flags-verification E2E queued by note above) is FALSE. There is NO frontend (or backend) memory blowup anywhere in the witness. OBJECTIVE EVIDENCE: (1) That run's `.memory_guard.json` recorded violation=null, peak=null, status=running — a guard RSS kill sets returncode=137 (GUARD_RETURN_CODE), timeout=124; the run exited rc=15 = raw external TerminateProcess (collateral of concurrent-lane guard/orphan cleanup or host memory pressure, M25/M26 class), NOT a guard OOM. (2) Independent profiling under a permissive guard w/ samples-jsonl: WARM shared-cache full numpy+scipy frontend closure lowers with the `molt build` process peaking 0.13–0.43GB (reached the exact same "source-recompiled external native packages" point the 07:18 run died at, at ~0.25GB, and continued); COLD isolated-cache re-lowering of 258 numpy+scipy modules stayed FLAT at ~0.03GB; per-module frontend timing ≤1.4s / ≤0.2GB, no giant module. (3) PRODUCTION HARNESS proof: prior full witness build RUN 20260709T045445 was guard-recorded at peak_total=1.85GB for the ENTIRE build (frontend + wasm cargo backend + link) — the whole witness fits under 2GB; the 12GB/18GB guard was never close. CONCLUSION: no memory reduction is warranted (would be theater, M05); do NOT lower/raise the guard for this. REAL FRONTIER: 20260709T045445 built app.wasm successfully and failed at RUNTIME (`_multiarray_umath: static-link Py_mod_exec ... cannot get sys.flags`), the frontier just LANDED as f3b97fa194 (row above); the witness's next blocker is the runtime C-ext-init frontier PAST sys.flags, found by re-running the E2E on clean origin/main. CURRENT MECHANICAL BLOCKER (transient, NOT origin/main, NOT this lane): the `C:\Molt\molt-src` WORKING TREE has uncommitted WIP breaking the wasm cargo backend (`error[E0432]: unresolved import super::super::super::DispatchMode` in molt-backend-wasm) — any proof_queue witness build from molt-src fails at cargo until its owner reverts/lands it (origin/main is clean: `git grep super::super::super::DispatchMode origin/main` = empty). Verification RUN 20260709T080709-pact-witness-acceptance-f8cdb2af43334822 (production 12/18 guard) confirmed frontend passing at low RSS. RECOMMENDATION: redirect E1 from the phantom memory fix to the post-sys.flags runtime frontier on clean code; separately harden the ~29-min witness build against cross-lane collateral termination. |
| E1-NUMPY-HEADER-OVERLAY-CUSTODY | Codex/codex-e1-numpy-header-overlay-custody-20260709 | 2026-07-09T09:04:41Z | CLAIMED | Reclaim stale NumPy header-overlay custody lane. Aperture: source-extension include resolution must derive numpy headers from package custody (_core/include) and delete Molt-owned include/numpy overlay; no third-party header reinvention, no cpython-abi/witness E2E lane edits. |
| E1-NUMPY-HEADER-OVERLAY-CUSTODY | Codex/codex-e1-numpy-header-overlay-custody-20260709 | 2026-07-09T10:29:43Z | COMPLETE | Landed bfbc23e8b6: deleted Molt-owned include/numpy overlay and _numpyconfig source-plan overlay; source/cpython ABI tiers no longer shadow package numpy headers; fail_closed_gate ecosystem_baked=0 and ecosystem_build_crutch=1. Proofs: py_compile touched files; ruff touched files; check_ecosystem_compat OK; fail_closed_gate OK; pytest cli extension + fail_closed_gate + memoryview + release_exit_gate + ecosystem compat = 150 passed. Queue: 20260709T100415 pact-witness-acceptance built+linked split WASM and reached runtime; no candidate_outputs.npz because next external frontier is _multiarray_umath Py_mod_exec nonzero after PyDict_SetItem(unresolved value). |
| E1-WITNESS-TO-GREEN | claude/opus-e1-multiarray-umath-20260709 | 2026-07-09T11:30:00Z | PROGRESS | `_multiarray_umath` "PyDict_SetItem(unresolved value)" frontier (row above) ROOT-CAUSED to the EXACT site + mechanism, and the TASK PREMISE ("a C-API function returns the unresolved value; implement it faithfully") is DISPROVEN with evidence — it is NOT a missing/stubbed C-API function; it is a split-runtime data-symbol duplication in the wasm linker. LANDED b9c3d322e1 (runtime/cpython-abi, in-lane, no ABI change, 19 lib tests + 2 new regressions green, clippy clean on touched files, gen_wasm_abi --check OK, drift gate PASS): two diagnostics that turn the opaque failure precise — PyDict_SetItemString now records the string KEY on failure, and describe_unresolved_pyobject classifies the unresolved value (exception-singleton by name / bare ob_type==NULL sentinel with pointer + runtime canonical Py_None/Py_True/Py_False addresses / type-object / instance). Replaying the built app.wasm+molt_runtime.wasm under MOLT_TRACE_CAPI=1 (no 19-min rebuild needed — the split app dynamically links the runtime by name) pinpoints the ABORT: numpy `_multiarray_umath_exec` -> `npy_cpu_features_dict()` -> `PyDict_SetItemString(dict, "MMX", Py_False)` returns -1 because `pyobj_to_handle(Py_False)` fails; the extension's `Py_False` resolves to addr 0x47804d9 (misaligned, and IDENTICAL to the extension's `PyExc_Exception` addr — a GOT artifact), NOT the runtime's canonical `Py_False`=0x2e1680, so the runtime's pointer-identity `pyobj_to_handle_static` misses it. (The earlier unchecked `PyDict_SetItemString(d,"error",PyExc_Exception)` at multiarraymodule.c:5046 fails the same way but is unchecked; the CHECKED abort is the cpu-features dict.) MECHANISM: numpy (cpython-abi header 1) references Py_None/Py_True/Py_False/PyExc_* as UNDEFINED data symbols; the split-runtime linker's alias (`_split_runtime_data_alias_object`/`_defined_runtime_data_symbol_offsets`/`_iter_linking_data_symbols` in tools/wasm_link.py) is meant to point them at the runtime's single copy, but it reads segment-RELATIVE offsets from the reloc (returns 0 for these sentinels) and the FINAL molt_runtime.wasm has no linking symbol table — so the alias cannot supply the final address and numpy links its own uninitialised copies. FIX BELONGS in the split-runtime wasm data-symbol/GOT linker (cli/wasm + backend-wasm HOT lane, M65-avoid, reserved/split-runtime orchestrator-adjacent per note M58) — NOT in runtime/cpython-abi; the bridge's unresolved-value check is CORRECT and was left unchanged (M05, no fake). HANDOFF: split-runtime linker owner — make the extension's cpython-abi data sentinels resolve to the runtime's single copy (export the data symbols + GOT-wire, or derive final addresses from the linked runtime, not the reloc). Diagnostic is now in-tree so any future run names the exact entry+value+addresses. Verified via wasm replay (diag_build app.wasm); full witness E2E blocked earlier on host-side scipy oracle in this manual harness (queue path unaffected). |
| P0-KWARG-CALL-TRAMPOLINE-ABI | claude/opus-p0-kwarg-trampoline-20260709 | 2026-07-09T21:21:35Z | COMPLETE | Landed 58928854b0 (origin/main FF from 7c82badf37). P0 silent-wrong-answer: the keyword-call lane garbled ALL of a compiled function's params into raw-bit float reinterpretations COLD on main -- f(1, d=4) for def f(a,d) returned (0.0, 1.09e-311) not (1,4); a keyword-only param poisoned even plain positional calls (f(1) for def f(a,*,d=9)). Positional-only calls were fine; warm default-root caches masked it. ROOT (LATENT, exposed by dc407f5d4d 'Reconcile WASM ABI dispatch...'): keyword / kw-only / arity-mismatch calls dispatch through the variadic trampoline lane call_function_obj_trampoline, which invokes the compiled ..__molt_trampoline_* entry with ABI fn(closure_bits, argv_ptr, argc). On native it resolved the target via function_required_call_target_ptr(func_ptr, tramp_ptr), which returns the function object's offset-8 FIXED-ARITY call_target slot whenever populated -- ignoring tramp_ptr. dc407f5d4d added init_runtime_callable_function_obj's native_direct_target path that caches the fixed-arity entry in that slot for app functions, so the trampoline lane fed the fixed-arity entry the variadic ABI: closure_bits(=0) became param0 and the raw argv pointer became param1 (reinterpreted as f64 -> tiny per-run-varying denormal). FIX: the trampoline lane now resolves tramp_ptr via runtime_callable_target_ptr (registry, canonicalization) with a raw-address fallback, never the fixed-arity slot; the offset-8 slot stays correct for the fixed-arity dispatch paths. Single fix point (molt_call_bind, molt_call_func_dispatch, call_function_objN, and the call-bind IC all converge here). VERIFIED COLD (fresh MOLT_CACHE, env cleared, plain molt build): probes p1-p8 all correct (p4/p6/p8 previously garbled now (1,4)/(1,4)/(1,9); p1/p2/p3/p5 unregressed); tests/differential/basic/args_kwargs.py + new regression tests/differential/basic/kwarg_call_lane.py PASS; 5 sibling kwargs differential files PASS; cargo test -p molt-runtime call::function = 13 passed incl new trampoline_lane_dispatches_trampoline_not_fixed_arity_call_target; check_table_drift PASS; push drift gate PASS. Witness-relevant: numpy/scipy use kw calls pervasively. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-pyobj-resolve-20260709 | 2026-07-10T00:20:00Z | PROGRESS | THREE `_multiarray_umath` Py_mod_exec frontiers CLEARED in sequence, all landed (this push, 7 commits onto dd08e8a015): (1) `PyDict_SetItem(unresolved KEY)` — the preserved e1-witness-pyobj-resolve fix VERIFIED on the merits and kept: numpy references builtin type statics (&PyLong_Type, ...) by ADDRESS as dict keys in its scalar->DType registry without PyType_Ready-ing them (already READY from init_static_types), so they never hit the PyType_Ready bridge registration; register_static_abi_objects now registers all 34 canonical Py*_Type statics (48788f0695, lock-step drift gate vs wasm_cpython_abi_data_symbol_names in test_wasm_split_runtime_got_data_bridge.py + 2 Rust regressions). (2) `Py_BuildValue string argument is NULL` — CPython-spec bug in BOTH hand-synced authorities (M45): 's'/'y' with NULL must yield None like 'z' (verified vs docs.python.org/3.12/c-api/arg.html; numpy dtypemeta passes alias=NULL via "Os"); fixed shims/pyarg_variadic.c (operative: numpy seal imports Py_BuildValue as runtime_symbol) + include/molt/Python.h dup (744048ae35). (3) `'str' object is not callable` + 54x `PyDict_SetItem(unresolved dict)` — TWO structural bridge gaps (6013b845be + 635d62f707): (a) tag_to_type mapped MoltTypeTag::Other (compiled functions/classes) to PyUnicode_Type, and PyObject_Call had NO route to invoke a Molt callable at all — numpy fetching numpy.dtypes._add_dtype_helper via GetAttrString got a str-masquerading proxy with no tp_call. Fix: new object_call RuntimeHooks entry routes bridge-managed callables through the single call authority (molt_callargs_new/push_pos/push_kw + molt_call_bind); Other now maps to PyBaseObject_Type; C-LAYOUT args tuples (shim PyTuple_New/Pack) are marshaled item-by-item into Molt tuples; raw-registered synthetic handles (0xA11C..., not MoltObject bits) excluded via bridge.molt_handle_for_pyobj; PyObject_CallMethod shim no longer bare-NULLs on non-empty formats (shared molt_callfunction_build_args). (b) CPython containers hold their OWN refs: PyDict_SetItem/PyList_Append now anchor stored key/value proxies (INCREF) and PyModule_AddObject retains the stolen ref — before, numpy's npy_cpu_dispatch_tracer_init (PyDict_New -> SetItemString -> Py_DECREF -> cache borrowed ptr) severed the cpu_dispatch_registry mapping (the argmin/argmax failure storm; AddObject could even drive a raw static's refcount to 0). All 12 affected symbols verified runtime_symbols in the numpy seal manifest — NO re-seal needed. PROOF: crate suite 24/24 (3 new regressions: type-static resolution + raw-handle exclusion + dict-anchor survival + Other!=str); got-data-bridge/symbol-authority/wasm-link 118/118; fail_closed_gate OK; drift gate PASS; fast replay (run 20260709T215403 artifacts, MOLT_TRACE_CAPI=1) confirmed each frontier fell in order. WITNESS STATE (E2E RUN_ID 20260709T230417-pact-witness-acceptance-3c54640ba4244693, rc=1): trace now COMPLETELY CLEAN of silent failures (was 54+1) — numpy _multiarray_umath init runs the full scalar-type table (102 PyType_Ready incl. dtypes.BoolDType), imports numpy.dtypes, and INVOKES the compiled-Python _add_dtype_helper through the new call authority; terminal is now `Py_mod_exec ... object has no attribute` = getattr on a RAW-REGISTERED extension object: DType (numpy's PyArray_BoolDType DTypeMeta static) crosses into Python code as the bridge's synthetic 0xA11C raw-token bits, which decode as a non-pointer (float-looking) so `DType.__name__` finds no heap object (bare AttributeError at attributes mutation/get null-ptr guard). NO candidate_outputs.npz yet — witness NOT green. NEXT FRONTIER (correct fix, foreign-object custody class): give raw-registered C objects a first-class Molt representation — mint a foreign/extension heap object (new TYPE_ID wrapping the C pointer, alloc'd via a runtime hook at register_raw_pyobj time) whose getattr/setattr/call route back through the extension object's own type slots (tp_getattro/tp_descr_get/tp_call) at the ABI boundary, replacing the synthetic counter handles entirely. That closes DType.__name__ AND the general "extension object used from Python" class (ndarray methods, dtype instances) which field_solve needs regardless. |
| PERF-HOTSPOT-SWEEP-M09 | claude/opus-perf-sweep-20260710 | 2026-07-10T03:15:00Z | COMPLETE | Landed docs/agent/PERF_HOTSPOTS_20260710.md — ranked MEASURE-ONLY witness-loop hotspot report. TOP-5: (1) P0 runtime wasm DUAL-crate-type rebuild: staticlib+cdylib as 2 separate `cargo rustc` passes (runtime_build.py:1973-1976), ~105-147s EACH (~230s/iteration), triggered EVERY E1 cpython-abi commit (molt-runtime deps molt-cpython-abi Cargo.toml:166; runtime fingerprint closure incl src/api/*.rs + shims/ per runtime_fingerprints.py:310) in a COLD per-session target dir. Evidence: .molt_cache/runtime_wasm=3.0GB/60 artifacts/33 distinct fingerprints-today = 33 invalidations/day; proof-queue runs with rebuilds=2 (780-1754s) vs rebuilds=0 cache-hit (453-683s). Fix specs: single-pass `--crate-type=staticlib,cdylib` (or drop cdylib if split-runtime needs only reloc) + M09 stable/incremental target dir keyed on runtime-fingerprint-family. (2) P0 runtime built at release-output heaviest-opt during correctness iteration (cargo_profiles.py:50-54) — iteration-scoped dev-fast knob. (3) P0 ~480s hidden frontend-lowering phase UNATTRIBUTED: witness build never sets MOLT_BUILD_DIAGNOSTICS so phase_sec/frontend_lowering_cache.hit_rate/runtime_wasm_cache go uncaptured — HANDOFF to E1 owner (tools/pact_witness_acceptance.py is E1-WITNESS-TO-GREEN solo-lane): set MOLT_BUILD_DIAGNOSTICS=1 + _FILE in `_build_wasm`. (4) P1 ~38MB split wasm (app 29212KB + molt_runtime 9358KB). (5) P2 ~7.2GB unbounded shared caches (runtime_wasm 3.0GB + module_lowering 4.2GB/5449 entries). Measure-only; did NOT edit E1 (witness harness) or CODEX-B-CACHE-KEY (cache_fingerprints/frontend_*) owned surfaces (CODEX-B already COMPLETE b9d6963fb). Native/wasm steady-state regression check (kw-call/INCREF hot-path) DEFERRED — live witness held the build slot; not fabricated. Evidence: logs/proof_queue/runs/*.log (12 runs) + .molt_cache disk measurements. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-foreign-custody-20260710 | 2026-07-10T04:05:00Z | PROGRESS | FOREIGN-OBJECT CUSTODY LANDED (39a4f737ee, rebased on 51a4a80042). The `DType.__name__` frontier (opus-e1-pyobj-resolve row above) CLEARED: numpy `_multiarray_umath` now runs PAST `_add_dtype_helper` into dtype registration. MECHANISM: a genuine C-extension `PyObject` crossing INTO compiled Python now gets a first-class Molt heap wrapper (`TYPE_ID_FOREIGN`, object/foreign.rs) minted at the bridge crossing points (`ObjectBridge::molt_value_for_pyobj` — call args, PyDict_SetItem/PyList_Append values, attr/call results), replacing the undecodable synthetic `0xA11C` token. Wrapper holds a strong ref on the C object (INCREF at mint, DECREF at drop via `foreign_drop`->`molt_foreign_object_release`) with c_ptr<->wrapper identity round-tripping back to C. getattr/setattr/call route through the object's OWN CPython type slots (`molt_foreign_{getattr,setattr,call}` -> tp_getattro/tp_setattro/tp_call), wired at the SHARED runtime helpers EVERY entry point funnels through: `attr_lookup_ptr` (ALL getattr paths — a single-point `molt_get_attr_name` arm was the bug: compiled `DType.__name__` uses `molt_get_attr_generic`, not `molt_get_attr_name`), `molt_set_attr_generic` (setattr), `molt_call_bind`+`call_callable0/1/2/3` (calls). CRITICAL SUBTLETY: type name-dunders (`__name__`/`__qualname__`) resolve HOOKS-FREE — C `tp_name` read directly + runtime `alloc_string` — because the getattr's ABI RuntimeHooks table can be a STUB in whichever split-runtime module the getattr lands in (`str_data` returned "" -> silently broke every hook-based name read; `type_getattro` was never even reached, proven by a thread-local diag). PyDict_SetItem/PyList_Append now foreign-wrap genuine C-object VALUES (numpy stores dtype instances). PROOF: cpython-abi crate suite 26/26 (+2 foreign identity/custody regressions; fixed stale test_bridge Other->object assertion + test_modules missing object_call/foreign_new hook fields); got-data-bridge 6/6; runtime+abi compile+clippy clean on touched files. Pre-existing UNRELATED red: `test_wasm_runtime_export_no_mangle` `molt_ws_pair` (websocket.rs — NOT my code, verified on origin/main) — spawned as a separate task. WITNESS STATE (E2E RUN_ID 20260710T033748-pact-witness-acceptance-ae136709e9574896, rc=1): trace CLEAN of foreign-object silent failures — `DType.__name__` resolves, `_add_dtype_helper` completes, `setattr(dtypes,'BoolDType',DType)` + `PyDict_SetItem(bool -> BoolDType instance)` succeed. NO candidate_outputs.npz — witness NOT green. NEXT FRONTIER (DISTINCT CLASS, not foreign-object custody): a WASM call-indirect `null function or function signature mismatch` trap during continued `_multiarray_umath` init (stack crosses the split-runtime app<->runtime module boundary via `runtimeImports.<computed>`, run_wasm.js:4996) — a function-pointer/split-runtime call-table issue (M40/M50-adjacent), NOT an undecodable-handle or missing-C-API problem; needs a call-indirect diagnostic to name the exact trapping funcref. |
| PACT-CONTRACT-LEDGER | claude/opus-pact-ledger-011-20260710 | 2026-07-10T05:30:00Z | COMPLETE | DOC-ONLY landing (one push, worktree off origin/main, rebased, SSH, drift gate PASS): the owed pact-collab correspondence + doc obligations. Three files: (1) `collab/pact/011_molt_reply_progress_sync_and_harness_proposal_20260710.md` — the molt-authored reply owed since 007 (009 §5 + 010 explicitly requested it): honest Kernel-A progress sync (numpy/scipy frontier chain landed this session — pure-Python closures `e031e4b02b`/`9d86b61f03`, C-ext seals `1aec825e02`/`02b5fa7dbd`, `sys.flags` `f3b97fa194`, GOT retarget `596d8baa8e`, type-statics/BuildValue-NULL/call-authority/anchors `48788f0695`/`744048ae35`/`6013b845be`/`635d62f707`, foreign-object custody `39a4f737ee`, kw-call P0 `58928854b0` — **NOT green**, halts at split-runtime call-indirect trap RUN_ID 20260710T033748-pact-witness-acceptance-ae136709e9574896 rc=1); the parity-harness interface for Kernel C/D..7 (per-kernel `<k>.py`+`make_<k>_fixture.py`+`<k>_reference.npz`+declarative `<k>_gates.json` mirroring Kernel A's exact/exact_set/atol/order-robust gates + shared `check_parity.py`); 010 work-package ack with converged W3→W1→P1 ranking; EMBED-API flagged done-but-unproven. (2) `collab/pact/STATUS.md` refresh 2026-07-01→2026-07-10 (live-frontier section rewritten from CLAIMS row 186 + git log, cited commits/RUN_IDs; historical 2026-07-01 block preserved). (3) `docs/PACT_SUPPORT_MATRIX.md` — the thrice-asked (008 §4/009 P1/010 W1a) {WASM-CPU,WebGPU}×{headless,browser} matrix, evidence-based per cell: WASM-CPU×headless=supported (witness parity in-flight); WASM-CPU×browser=needs-port (node-proven, browser E2E uncaptured); **WebGPU×headless=blocked** (no node WebGPU binding — grep-zero `@webgpu/dawn`/`wgpu-native`; JS-mock dispatcher only; native Rust wgpu is a separate untested lane); WebGPU×browser=needs-port (WGSL shaders shipped, on-GPU parity uncaptured per doc-71 exit#7). Contest-legal target = WASM-CPU/native; WebGPU showcase-only. Ledger rows RESPOND+MATRIX→done; roll-up 4→6 done / 12→10 not-started; KERN-CD unblocked (pact side). Evidence: files on disk + cited landed commits + queue RUN_ID. NO builds run (doc-only lane). |
| PACT-CONTRACT-LEDGER | claude/opus-pact-ledger-20260710 | 2026-07-10T05:00:00Z | COMPLETE | Compiled the authoritative pact-collab exit-criteria tracker `docs/agent/PACT_CONTRACT_LEDGER.md` from correspondence 001-010 + STATUS.md + README.md. 30 obligations, one row each with stable topic-based anchors + evidence: 4 done, 7 in-flight (incl. 4 standing constraints), 7 queued, 12 not-started. Priority ordering converged with 010 §4 (P0=Kernel A `KA` unchanged; post-A W3-ONNX -> W1/KERN7 -> P1 MATRIX -> W2 -> W4 -> W5; P3/P4 horizon). Includes a refresh protocol (update on every new 011+), a post-A pivot plan, and a surprising/under-tracked section. KEY FINDINGS (evidence-based): (1) STATUS.md is 9 days stale (dated 2026-07-01) and understates progress -- real live frontier is the split-runtime call-indirect trap (CLAIMS row 186, branch e1-callindirect-20260710), many frontiers past sys.flags/Py_BuildValue/foreign-custody; trust CLAIMS+git log over STATUS.md. (2) The {WASM-CPU,WebGPU}x{headless,browser} support matrix (008 §4 / 009 P1 / 010 W1a) asked 3x, still not-started, unowned -- cheapest high-leverage item. (3) W3 (ONNX trunk export, ranked #1 post-A by 010 §4) is completely unowned though the ONNX->WASM substrate ships (onnx_interpreter.py, matmul_f32_tiled, PaddleOCR) -- every non-P0 pact obligation is effectively unowned under the P0-monofocus. (4) EMBED-API "ask #3 DONE" is done-but-unproven (test_browser_embed_forward_roundtrips_float32_typed_arrays flagged Unknown in STATUS.md). (5) NUMPY-MATRIX (002) never published; docs/CAPABILITIES.md has 0 numpy/scipy. (6) No molt-authored reply since 007; RESPOND (parity-harness interface + 011 ack) owed. No calendar deadline exists; only the 30-min per-run contest budget. READ+WRITE-DOCS only, no builds. Landed origin/main FF, drift gate PASS. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-callindirect-20260710 | 2026-07-10T08:57:44Z | PROGRESS | SPLIT-RUNTIME CALL-INDIRECT NULL-FUNCREF TRAP (row 186 frontier) ROOT-CAUSED + FIX SET LANDED via ff to origin/main (branch e1-callindirect-20260710 rebased on fd23236508; drift+fail_closed+table-drift gates PASS; cpython-abi suite green -- test_type_ready_inheritance 11/11 incl. the mask-proof regression). ROOT CAUSE (a CLASS, not an instance): `PyObject_CallObject(callable, NULL)` forwarded a NULL args pointer straight into tp_call/tp_new instead of CPython's empty-tuple singleton (Objects/call.c routes NULL args through _PyObject_CallNoArgs). numpy `use_new_as_default` (dtypemeta.c) does `PyObject_CallObject((PyObject*)DTypeClass, NULL)` and the DType tp_new parses args AS A TUPLE -> the NULL stranded it -> the split-runtime call_indirect trapped. FIVE landed commits: (1) 33e97fc644 PyType_Ready establishes CPython tp_free/tp_alloc post-ready invariant -- numpy PyBoundArrayMethod_Type ships tp_free=NULL and its dealloc ends `Py_TYPE(self)->tp_free(self)`, which was call_indirect on table idx 0 (verified vs CPython 3.12.13 via ctypes: non-GC=PyObject_Free, GC=PyObject_GC_Del, tp_alloc=PyType_GenericAlloc); new no_mangle PyObject_Free export. (2) c9916c4cd3 install CPython type_call as PyType_Type.tp_call (numpy _DTypeMeta was `object is not callable` without it; molt_type_call ports Objects/typeobject.c::type_call verbatim incl. _Py_CheckFunctionResult fail-closed). (3) c8400400bd regen wasm link-import registries for PyObject_Free + dealloc-slot replay probe. (4) e9a1ee6ec6 PyObject_CallObject(c,NULL) delegates to PyObject_CallNoArgs (mints+frees the empty tuple) + MOLT_TRACE_CAPI molt_type_call diagnostic. (5) 819151222a mask-proof regression: recording tp_new asserts args NON-NULL AND len-0 tuple. E2E PROOF (RUN 20260710T074320): `molt_type_call:new(numpy.dtypes.StringDType tp_new=0x3f6c args_null=false)` then `molt_type_call:result(-> StringDType)`. Frontier ADVANCED past the ENTIRE abstract DType family to StringDType. NEW FRONTIER (DISTINCT CLASS, run_wasm.js:306): `_multiarray_umath: Py_mod_exec slot returned non-zero: Instantiating <DType> did not return a dtype instance`. numpy dtypemeta.c does `res = PyObject_CallObject((PyObject*)DTypeClass, NULL); if (!PyObject_TypeCheck(res, expected_dtype_meta)) raise`. tp_new now RUNS (args_null=false) but the returned object fails numpy's dtype-instance PyObject_TypeCheck across molt's foreign-object custody -- JOB 2 in progress: verifying type-identity preservation of the tp_new result across the C<->Molt crossing (TYPE_ID_FOREIGN wrapper vs the real C DTypeMeta). |
| E1-WITNESS-TO-GREEN | claude/opus-e1-callindirect-20260710 | 2026-07-10T09:20:14Z | PROGRESS | DTYPE-INSTANCE TYPE-CHECK FRONTIER (run_wasm.js:306 `Instantiating <DType> did not return a dtype instance`) ROOT-CAUSED + FIX LANDED (fcb1d9596d). The custody hypothesis is DISPROVEN by evidence: the object numpy's tp_new returns IS the genuine C descriptor with the correct Py_TYPE (trace `molt_type_call:result(-> StringDType)`); no foreign wrapper is involved. TRUE ROOT CAUSE (a CLASS, weakened-assert per M05): molt's `PyObject_TypeCheck` (runtime/molt-cpython-abi/src/api/typeobj.rs) answered ONLY the exact-type arm `Py_TYPE(op)==tp`, dropping CPython's subtype arm. CPython (Include/object.h): `PyObject_TypeCheck(ob,tp) = Py_IS_TYPE(ob,tp) || PyType_IsSubtype(Py_TYPE(ob),tp)`. numpy `use_new_as_default` (dtypemeta.c:91-110, v2.4.2 on-disk source) does `res = PyObject_CallObject((PyObject*)DTypeClass, NULL); if (!PyArray_DescrCheck(res)) raise`, and `PyArray_DescrCheck(op)` is `PyObject_TypeCheck(op, &PyArrayDescr_Type)` (ndarrayobject.h:32). A DType descriptor's Py_TYPE is its concrete DType class (numpy stringdtype/dtype.c:1030 sets `StringDType.tp_base = &PyArrayDescr_Type`; PyArrayDescr_Type is `numpy.dtype`), NEVER PyArrayDescr_Type itself -> exact-only returned 0 -> every genuine descriptor rejected. ABI-TIER EVIDENCE: the sealed `_multiarray_umath` manifest is `abi_tier=cpython-abi`, which compiles against `runtime/molt-cpython-abi/include/Python.h` whose `PyObject_TypeCheck` macro calls the extern Rust export (NOT the opaque-layout source-compat header inline); numpy imports `PyObject_TypeCheck` as a runtime symbol 43x, so this one Rust export is the live authority and a runtime-only rebuild (no numpy recompile) exercises it. FIX mirrors CPython verbatim: `Py_IS_TYPE(op,tp) || PyType_IsSubtype(Py_TYPE(op), tp)` (PyType_IsSubtype already walks the tp_base chain; StringDType->PyArrayDescr_Type is one hop). Mask-proof regression `typecheck_matches_base_type_like_pyarray_descrcheck` asserts the SUBTYPE arm (instance-vs-base==1, which exact-only returns 0) + exact-match + a no-over-match negative; test_type_ready_inheritance 12/12; clippy clean on touched lines; drift+fail_closed+table-drift gates PASS. E2E frontier-advance confirmation IN-FLIGHT (proof_queue pact-witness-acceptance rebuilds runtime with the fix + replays numpy init); next-frontier reported once it lands. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-callindirect-20260710 | 2026-07-10T09:30:48Z | PROGRESS | TYPE-CHECK CLASS FIX COMPLETED (c0b67fbe22, landed FF on main): the same exact-match-instead-of-subtype bug lived in THREE sibling authorities, not one. Beyond PyObject_TypeCheck (5f4897ec46), molt's `PyObject_IsInstance` (typeobj.rs) and `PyObject_IsSubclass` (object.rs) also answered only pointer-identity. CPython reduces (for a type arg) `PyObject_IsInstance(inst, cls)` -> `PyObject_TypeCheck(inst, cls)` and `PyObject_IsSubclass(a, b)` -> `PyType_IsSubtype(a, b)` (Objects/abstract.c recursive_isinstance/issubclass). numpy imports both as runtime symbols (IsInstance 15x -- e.g. descriptor.c:497 `PyObject_IsInstance(conv, &PyArray_StringDType)`; IsSubclass 7x), so both were latent next-frontier blockers of the identical class. IsInstance now delegates to the fixed PyObject_TypeCheck; IsSubclass to PyType_IsSubtype. Both only POINTER-compare the class arg (deref the instance/derived side only), so the __instancecheck__/__subclasscheck__/tuple cases Molt cannot resolve here keep the conservative 0 (never a false positive). Two more mask-proof regressions (isinstance base-type subtype arm; issubclass base-chain walk + directionality base-not-subclass-of-derived); test_type_ready_inheritance 14/14; clippy clean on touched lines; drift+fail_closed+table-drift gates PASS. NOTE: the in-flight E2E isolates the PyObject_TypeCheck fix (molt-src main carried only that when the run fired); IsInstance/IsSubclass share its exact PyType_IsSubtype mechanism, verified by crate tests. E2E next-frontier still reported once the witness run lands. |
| E1-WITNESS-TO-GREEN | claude/opus-e1-pydict-key-custody-20260710 | 2026-07-10T11:22:19Z | PROGRESS | PyDict_SetItem KEY foreign-custody CLASS FIX LANDED (04599327e2, ff on main; drift+fail_closed+table-drift gates PASS; cpython-abi dict tests 2/2 incl. mask-proof `dict_set_item_gives_foreign_custody_to_key`). ROOT CAUSE (a CLASS, confirmed by trace; the foreign-custody hypothesis from row 39a4f737ee held): `PyDict_SetItem` minted a first-class `TYPE_ID_FOREIGN` wrapper for an unresolved VALUE (39a4f737ee) but dropped an unresolved KEY with a bare -1 and NO pending exception. numpy `_multiarray_umath` exec registers its builtin dtype/DType singletons (pure C `PyArray_Descr`, bridge-unknown) as `PyDict_SetItem` KEYS right after readying the DType classes -> silent -1 -> opaque "Py_mod_exec slot returned non-zero without setting an exception" at the FIRST DType (BoolDType). This is the frontier from RUN 20260710T092211 (generic no-silent-failure message on the wasm-opt'd runtime; the instrumented runtime surfaced the recorded silent failure). FIX (mirrors the value path): an unresolved key resolves via `molt_value_for_pyobj` (mints/reuses a foreign wrapper by stable C-pointer identity via `foreign_wrapper_for`, so later insert/lookup with the same singleton pointer resolves to the same handle), skip the redundant Py_INCREF on a foreign key (the wrapper already holds custody), and set an honest SystemError on the genuinely-unresolvable path instead of a contentless -1. TRACE EVIDENCE (MOLT_TRACE_CAPI replay of the witness app.wasm with an instrumented runtime, branch wip/e1-multiarray-instrumentation-20260710): last successful `PyType_Ready(numpy.dtypes.BoolDType)` -> `silent-failure PyDict_SetItem(unresolved key ...: instance-of '<unnamed>')` -> `Py_mod_exec:return(rc=nonzero exc_pending=false)`. E2E PROOF (same replay, fixed runtime): frontier ADVANCES 180->326 trace lines, 0 PyDict_SetItem silent failures, all 31 DType classes (BoolDType..StringDType + abstract DTypes) ready. NEW FRONTIER (DISTINCT CLASS, NOT dict custody): molt's `PyObject_Call` cannot invoke a foreign `numpy._DTypeMeta` class through its metatype `tp_call` -> `'numpy._DTypeMeta' object is not callable` (now exc_pending=TRUE, an honest exception) during `init_string_dtype`/StringDType instantiation. Instrumentation branch (wip/e1-multiarray-instrumentation-20260710) NOT landed to main; only the clean fix + regression landed. |
