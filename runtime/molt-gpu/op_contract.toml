# @generated by tools/gen_gpu_op_contract.py from the pinned upstream
# tinygrad 0.13.0 (bench/friends/repos/tinygrad_off_the_shelf). DO NOT EDIT.
#
# The GPU op-contract FACT (doc 67 Phase 1, fact family `gpu_op_contract`):
# per molt PrimitiveOp, its disposition against the pinned upstream Ops set +
# code_for_op renderer contract. Regenerate with
#   python3 tools/gen_gpu_op_contract.py
# and verify in CI with
#   python3 tools/gen_gpu_op_contract.py --check
# A mis-mapping, a changed upstream C-pattern, or a new upstream ALU op turns
# this RED. The reconciliation decisions live in the generator and are PROVEN
# against the pinned source (e.g. CMOD really is code_for_op[({a}%{b})]; MAX
# really is NOT a code_for_op entry).

[meta]
pinned_tinygrad_version = "0.13.0"
ops_enum_source = "bench/friends/repos/tinygrad_off_the_shelf/tinygrad/uop/__init__.py"
code_for_op_source = "bench/friends/repos/tinygrad_off_the_shelf/tinygrad/renderer/cstyle.py"
rewrite_source = "bench/friends/repos/tinygrad_off_the_shelf/tinygrad/uop/decompositions.py"
upstream_ops_member_count = 93
upstream_alu_member_count = 28
molt_primitive_op_count = 26

# === molt PrimitiveOp -> upstream disposition (ops.rs::ALL order) ===

[[primitive]]
molt_op = "Add"
disposition = "mapped"
upstream_op = "ADD"
renderer_c_pattern = "({a}+{b})"
dtype_rule = "same as promoted operand dtype"
ieee_edge = "IEEE-754 addition; NaN propagates; (+inf)+(-inf)=NaN"
note = "code_for_op[Ops.ADD] = ({a}+{b})."

[[primitive]]
molt_op = "Sub"
disposition = "mapped"
upstream_op = "SUB"
renderer_c_pattern = "({a}-{b})"
dtype_rule = "same as promoted operand dtype"
ieee_edge = "primitive subtraction (NOT Add(a, Neg(b))); preserves -0.0"
note = "code_for_op[Ops.SUB] = ({a}-{b}). Upstream ALSO synthesizes SUB from x + Neg(y) when the renderer supports SUB (decompositions.py:488); molt exposes it as a first-class primitive, matching the renderer entry."

[[primitive]]
molt_op = "Mul"
disposition = "mapped"
upstream_op = "MUL"
renderer_c_pattern = "({a}*{b})"
dtype_rule = "same as promoted operand dtype"
ieee_edge = "IEEE-754 multiply; NaN propagates; 0*inf=NaN"
note = "code_for_op[Ops.MUL] = ({a}*{b})."

[[primitive]]
molt_op = "Idiv"
disposition = "mapped"
upstream_op = "CDIV"
renderer_c_pattern = "({a}/{b})"
dtype_rule = "integer dtype (truncating C division)"
ieee_edge = "integer division truncating toward zero (C semantics): (-7)/3 == -2. Divide-by-zero is UB at the C level / guarded above."
note = "RECONCILED §1.2.1: molt names this `Idiv`; upstream names it `Ops.CDIV` (C truncating divide). code_for_op[Ops.CDIV] = ({a}/{b}). The molt name and the upstream name diverge in SPELLING ONLY — same C-pattern, same semantics. Python `//` (FLOORDIV, floor toward -inf) is a SEPARATE composed op (see the FloorDiv entry in the upstream-only section)."

[[primitive]]
molt_op = "Mod"
disposition = "mapped"
upstream_op = "CMOD"
renderer_c_pattern = "({a}%{b})"
dtype_rule = "integer dtype (truncating C remainder)"
ieee_edge = "C remainder; result has the sign of the dividend: (-7)%3 == -1. Mod-by-zero is UB at the C level / guarded above."
note = "RECONCILED §1.2.1: molt names this `Mod`; upstream names it `Ops.CMOD` (C truncating remainder). code_for_op[Ops.CMOD] = ({a}%{b}). Spelling diverges only. Python `%` (FLOORMOD, sign of divisor) is a SEPARATE composed op (see the FloorMod entry in the upstream-only section)."

[[primitive]]
molt_op = "Neg"
disposition = "mapped"
upstream_op = "NEG"
renderer_c_pattern = "-{x}"
dtype_rule = "same as operand dtype"
ieee_edge = "flips the IEEE sign bit (NOT a*-1); -(-0.0)=+0.0; -(NaN) flips NaN sign"
note = "code_for_op[Ops.NEG] = -{x}."

[[primitive]]
molt_op = "Cmplt"
disposition = "mapped"
upstream_op = "CMPLT"
renderer_c_pattern = "({a}<{b})"
dtype_rule = "output dtype is ALWAYS bool (GroupOp.Comparison)"
ieee_edge = "IEEE unordered: NaN < x is false"
note = "code_for_op[Ops.CMPLT] = ({a}<{b}). Ops.CMPLT in GroupOp.Comparison."

[[primitive]]
molt_op = "Cmpeq"
disposition = "mapped"
upstream_op = "CMPEQ"
renderer_c_pattern = "({a}=={b})"
dtype_rule = "output dtype is ALWAYS bool (GroupOp.Comparison)"
ieee_edge = "IEEE: NaN == NaN is false"
note = "code_for_op[Ops.CMPEQ] = ({a}=={b}). Ops.CMPEQ in GroupOp.Comparison."

[[primitive]]
molt_op = "Cmpne"
disposition = "mapped"
upstream_op = "CMPNE"
renderer_c_pattern = "({a}!={b})"
dtype_rule = "output dtype is ALWAYS bool (GroupOp.Comparison)"
ieee_edge = "IEEE: NaN != NaN is true"
note = "code_for_op[Ops.CMPNE] = ({a}!={b}). Ops.CMPNE in GroupOp.Comparison."

[[primitive]]
molt_op = "And"
disposition = "mapped"
upstream_op = "AND"
renderer_c_pattern = "({a}&{b})"
dtype_rule = "integer/bool dtype"
ieee_edge = "bitwise; not defined for floats"
note = "code_for_op[Ops.AND] = ({a}&{b})."

[[primitive]]
molt_op = "Or"
disposition = "mapped"
upstream_op = "OR"
renderer_c_pattern = "({a}|{b})"
dtype_rule = "integer/bool dtype"
ieee_edge = "bitwise; not defined for floats"
note = "code_for_op[Ops.OR] = ({a}|{b})."

[[primitive]]
molt_op = "Xor"
disposition = "mapped"
upstream_op = "XOR"
renderer_c_pattern = "({a}^{b})"
dtype_rule = "integer/bool dtype"
ieee_edge = "bitwise; not defined for floats"
note = "code_for_op[Ops.XOR] = ({a}^{b})."

[[primitive]]
molt_op = "Shl"
disposition = "mapped"
upstream_op = "SHL"
renderer_c_pattern = "({a}<<{b})"
dtype_rule = "integer dtype"
ieee_edge = "logical left shift; shift-count UB guarded above"
note = "code_for_op[Ops.SHL] = ({a}<<{b})."

[[primitive]]
molt_op = "Shr"
disposition = "mapped"
upstream_op = "SHR"
renderer_c_pattern = "({a}>>{b})"
dtype_rule = "integer dtype"
ieee_edge = "arithmetic right shift for signed (sign-extending), logical for unsigned (zero-filling); shift-count UB guarded above"
note = "code_for_op[Ops.SHR] = ({a}>>{b})."

[[primitive]]
molt_op = "Exp2"
disposition = "mapped"
upstream_op = "EXP2"
renderer_c_pattern = "exp2({x})"
dtype_rule = "float dtype"
ieee_edge = "exp2(NaN)=NaN; exp2(+inf)=+inf; exp2(-inf)=0.0"
note = "code_for_op[Ops.EXP2] = exp2({x}). Transcendental: ULP budget vs host libm pinned in docs/spec/tinygrad_pin.md (numeric oracle, doc 67 Phase 2)."

[[primitive]]
molt_op = "Log2"
disposition = "mapped"
upstream_op = "LOG2"
renderer_c_pattern = "log2({x})"
dtype_rule = "float dtype"
ieee_edge = "log2(NaN)=NaN; log2(+inf)=+inf; log2(0.0)=-inf; log2(x<0)=NaN"
note = "code_for_op[Ops.LOG2] = log2({x}). Transcendental: ULP budget vs host libm pinned in docs/spec/tinygrad_pin.md."

[[primitive]]
molt_op = "Sin"
disposition = "mapped"
upstream_op = "SIN"
renderer_c_pattern = "sin({x})"
dtype_rule = "float dtype"
ieee_edge = "sin(NaN)=NaN; sin(+-inf)=NaN"
note = "code_for_op[Ops.SIN] = sin({x}). Transcendental: ULP budget vs host libm pinned in docs/spec/tinygrad_pin.md."

[[primitive]]
molt_op = "Sqrt"
disposition = "mapped"
upstream_op = "SQRT"
renderer_c_pattern = "sqrt({x})"
dtype_rule = "float dtype"
ieee_edge = "sqrt(NaN)=NaN; sqrt(+inf)=+inf; sqrt(-0.0)=-0.0; sqrt(x<0)=NaN"
note = "code_for_op[Ops.SQRT] = sqrt({x})."

[[primitive]]
molt_op = "Reciprocal"
disposition = "mapped"
upstream_op = "RECIPROCAL"
renderer_c_pattern = "(1/{x})"
dtype_rule = "float dtype only (use Idiv(1,a) for integers)"
ieee_edge = "RECIPROCAL(0.0)=+inf; RECIPROCAL(-0.0)=-inf; RECIPROCAL(NaN)=NaN"
note = "code_for_op[Ops.RECIPROCAL] = (1/{x}). Upstream also rewrites RECIPROCAL(x) -> FDIV(1, x) when the renderer supports FDIV (decompositions.py:506); the code_for_op entry is the renderer contract."

[[primitive]]
molt_op = "Trunc"
disposition = "mapped"
upstream_op = "TRUNC"
renderer_c_pattern = "trunc({x})"
dtype_rule = "float dtype"
ieee_edge = "trunc(NaN)=NaN; trunc(+-inf)=+-inf; truncates toward zero"
note = "code_for_op[Ops.TRUNC] = trunc({x}). Used in floor/ceil/round compositions."

[[primitive]]
molt_op = "Max"
disposition = "rewrite"
upstream_op = "MAX"
renderer_c_pattern = ""  # no code_for_op entry
lowers_to = "WHERE(CMPLT(a, b), b, a)"
dtype_rule = "same as promoted operand dtype"
ieee_edge = "NaN-propagating for floats in molt's renderer; integers use comparison. (Note: the upstream CMPLT-where rewrite is NOT NaN-propagating, so a renderer that owns a native NaN-propagating max() — MSL/WGSL — is closer to molt's documented contract; CUDA/HIP emit a guarded fmax.)"
note = "RECONCILED §1.2.1: Ops.MAX is a Binary ALU member but has NO code_for_op entry. Upstream produces it via a pattern rewrite (decompositions.py:465): `Ops.MAX -> (a < b).where(b, a)` when the renderer lacks MAX but has CMPLT; the integer-decomposition path (decompositions.py:380) is the same WHERE(CMPLT,...) shape. So `MAX` is a REWRITE, not a code_for_op primitive — the design's 'every renderer op is a primitive, no more no less' was FALSE here."

[[primitive]]
molt_op = "Where"
disposition = "mapped"
upstream_op = "WHERE"
renderer_c_pattern = "({a}?{b}:{c})"
dtype_rule = "result dtype = dtype of the two value operands"
ieee_edge = "pure select on the bool condition; no arithmetic edge"
note = "code_for_op[Ops.WHERE] = ({a}?{b}:{c}). Ternary select."

[[primitive]]
molt_op = "Cast"
disposition = "mapped"
upstream_op = "CAST"
renderer_c_pattern = ""  # no code_for_op entry
dtype_rule = "target dtype (FusedOp::dst_dtype)"
ieee_edge = "value-converting cast; float->int truncates; saturation per dtype"
note = "Ops.CAST is rendered by a dedicated base_rewrite pattern (render_cast), NOT a code_for_op ALU lambda; it is in GroupOp.Elementwise. Treated as a mapped primitive (the renderer owns its lowering)."

[[primitive]]
molt_op = "Bitcast"
disposition = "mapped"
upstream_op = "BITCAST"
renderer_c_pattern = ""  # no code_for_op entry
dtype_rule = "target dtype (FusedOp::dst_dtype), same bit width"
ieee_edge = "reinterpret bits, no value conversion"
note = "Ops.BITCAST is rendered by a dedicated base_rewrite pattern (__builtin_bit_cast), NOT a code_for_op ALU lambda; in GroupOp.Elementwise. Mapped primitive (renderer owns its lowering)."

[[primitive]]
molt_op = "ReduceSum"
disposition = "reduce"
upstream_op = "REDUCE"
renderer_c_pattern = ""  # no code_for_op entry
reduce_alu = "ADD"
dtype_rule = "accumulator dtype"
ieee_edge = "sum reduction; float associativity is implementation-ordered"
note = "Upstream REDUCE folds with Ops.ADD over the reduce axes."

[[primitive]]
molt_op = "ReduceMax"
disposition = "reduce"
upstream_op = "REDUCE"
renderer_c_pattern = ""  # no code_for_op entry
reduce_alu = "MAX"
dtype_rule = "accumulator dtype"
ieee_edge = "max reduction; NaN-propagating for floats"
note = "Upstream REDUCE folds with Ops.MAX over the reduce axes."

# === upstream GroupOp.ALU members NOT exposed as a molt PrimitiveOp ===
# Each is classified (composed / not_yet_supported + reason) so a new
# upstream ALU op cannot be silently absent. NOTE the §1.2.1 ops:
# FDIV/POW/FLOORDIV/FLOORMOD/MULACC (composed) + THREEFRY (not_yet_supported).

[[upstream_only_alu]]
upstream_op = "FDIV"
disposition = "composed"
lowers_to = "MUL(a, RECIPROCAL(b))  # molt DIV; upstream FDIV is the renderer-native form"
note = "Float true-division. Upstream FDIV is a Binary ALU member (in some renderers a native a/b on floats). molt composes float division as MUL(a, RECIPROCAL(b)) over its primitives; RECIPROCAL is a molt primitive. Upstream itself rewrites RECIPROCAL(x) -> FDIV(1,x) (decompositions.py:506) only when the renderer supports FDIV — the inverse direction. molt's primitive RECIPROCAL is the canonical form."

[[upstream_only_alu]]
upstream_op = "FLOORDIV"
disposition = "composed"
lowers_to = "CDIV(a,b) - (CMOD(a,b) != 0 & (a<0) != (b<0))  # Python // (floor toward -inf), decompositions.py:447"
note = "Python floor-division (// : floor toward -inf, distinct from C-trunc CDIV/molt Idiv). Upstream FLOORDIV is a Binary ALU member, in GroupOp.UnsafePad, with NO code_for_op entry; it is lowered to the CDIV-correction expression at decompositions.py:447 (and divandmod.py symbolic paths). molt composes it from its CDIV/CMOD/comparison primitives — it is the Python `//` operator, NOT molt's Idiv."

[[upstream_only_alu]]
upstream_op = "FLOORMOD"
disposition = "composed"
lowers_to = "let r = CMOD(a,b) in WHERE((r != 0) & ((r<0) != (b<0)), r + b, r)  # Python % (sign of divisor), decompositions.py:450-451"
note = "Python floor-modulo (% : result has sign of divisor, distinct from C-trunc CMOD/molt Mod). Upstream FLOORMOD is a Binary ALU member with NO code_for_op entry; lowered at decompositions.py:450-451 (and divandmod.py). molt composes it from its CMOD/comparison/where primitives — it is the Python `%` operator, NOT molt's Mod."

[[upstream_only_alu]]
upstream_op = "MULACC"
disposition = "composed"
lowers_to = "ADD(MUL(a, b), c)  # fused multiply-accumulate, decompositions.py:501"
note = "Fused multiply-accumulate. Upstream MULACC is a Ternary ALU member; it is a FUSION of ADD(MUL(a,b),c) introduced by a rewrite (decompositions.py:501) only when the renderer supports MULACC. molt expresses a*b+c as ADD(MUL(...)) primitives and fuses in its own LazyOp layer — no separate primitive needed (the spec's explicit drop, now PINNED as a composition rather than a free-floating prose choice)."

[[upstream_only_alu]]
upstream_op = "POW"
disposition = "composed"
lowers_to = "EXP2(MUL(LOG2(a), b))  # x**y = 2**(y*log2(x)) for x>0; sign/edge handling per composition"
note = "Power. Upstream POW is a Binary ALU member; in GroupOp.UnsafePad. molt composes it from EXP2/LOG2/MUL primitives (with the standard x**y decomposition + sign/zero edge handling). No code_for_op entry upstream."

[[upstream_only_alu]]
upstream_op = "THREEFRY"
disposition = "not_yet_supported"
reason = "Counter-based PRNG (Threefry 2x32). A specialized random-number generation op, not an elementwise math primitive; upstream lowers it via a dedicated threefry2x32 rewrite (decompositions.py:463) when the renderer lacks it. molt does not yet expose tensor RNG through the primitive set; when it does, this becomes a composed/rewrite entry. Explicit, reasoned gap — fail-closed, never silently approximated."
note = "Upstream THREEFRY is a Binary ALU member (RNG), no code_for_op entry."

# === upstream Ops deliberately NOT modeled as compute primitives ===
# tensor-core / movement / scheduler ops molt owns at a different layer.
# NOTE the §1.2.1 op: WMMA (tensor-core, rendered by its own pattern).

[[upstream_non_primitive]]
upstream_op = "WMMA"
scope = "tensor-core"
note = "RECONCILED §1.2.1: Ops.WMMA (warp-matrix-multiply-accumulate, tensor core) is NOT an elementwise compute primitive — it is a hardware tensor-core instruction rendered by its own base_rewrite pattern (cstyle.py:62, __WMMA(...)), not a code_for_op ALU lambda. molt models matmul as a composition/schedule over its primitives + a device-level tensor-core path, NOT as a PrimitiveOp. Out of the primitive-set scope by design; recorded here so it is a CLASSIFIED omission, not a silent one."
