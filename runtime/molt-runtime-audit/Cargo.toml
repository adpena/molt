[package]
name = "molt-runtime-audit"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(feature, values("refcount_verify"))'] }

[features]
default = []
refcount_verify = []

[lib]
path = "src/lib.rs"
