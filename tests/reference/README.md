# Reference Tests

This directory holds regression coverage for the reference-lane scaffolding.

The tests intentionally exercise only manifest loading, lane-path validation,
and command-line plumbing. They are a guardrail for the directory layout and
file formats, not a benchmark implementation.
