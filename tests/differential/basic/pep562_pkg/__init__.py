# Companion package for tests/differential/basic/pep562_module_getattr_dir.py.
# Not a standalone differential (lives in a subdir, so the suite-layout lane
# check does not collect it). Holds the PEP-562 fixture modules.
