# tinygrad Reference Lane

This directory is reserved for the Falcon `falcon-1` tinygrad reference lane.

Keep lane-local code, pinned inputs, and adapter notes here so future reference
work can extend this lane without changing the surrounding layout.

At the scaffold stage this directory only needs to exist and validate cleanly.
