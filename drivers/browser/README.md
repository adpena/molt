# Browser Drivers

General browser deployment lanes live here.

Target classes:
- `wasm_cpu/`: browser-hosted wasm without GPU acceleration
- `webgpu/`: browser-hosted wasm/WebGPU hybrid execution and bootstrap
