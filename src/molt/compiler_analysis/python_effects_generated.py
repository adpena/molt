# @generated by tools/gen_python_effects.py from
# runtime/python_effects.toml. DO NOT EDIT.
"""Raw-mask Python effect lattice shared by compiler analyses."""

from __future__ import annotations

from typing import Final, TypeAlias

EffectMask: TypeAlias = int
EFFECT_MASK_BITS: Final = 32
NO_EFFECTS: Final[EffectMask] = 0
EXECUTES_ARBITRARY_PYTHON: Final[EffectMask] = 1 << 0
INVOKES_IMPORT_SYSTEM: Final[EffectMask] = 1 << 1
REFLECTS_NAMESPACE: Final[EffectMask] = 1 << 2
READS_MODULE_METADATA: Final[EffectMask] = 1 << 3
WRITES_MODULE_METADATA: Final[EffectMask] = 1 << 4
READS_GLOBAL_NAMESPACE: Final[EffectMask] = 1 << 5
WRITES_GLOBAL_NAMESPACE: Final[EffectMask] = 1 << 6
READS_FRAME_STATE: Final[EffectMask] = 1 << 7
WRITES_FRAME_STATE: Final[EffectMask] = 1 << 8
READS_OBJECT_STATE: Final[EffectMask] = 1 << 9
WRITES_OBJECT_STATE: Final[EffectMask] = 1 << 10
ALLOCATES: Final[EffectMask] = 1 << 11
RELEASES_REFERENCE: Final[EffectMask] = 1 << 12
RUNS_FINALIZER: Final[EffectMask] = 1 << 13
RUNS_WEAKREF_CALLBACK: Final[EffectMask] = 1 << 14
INVOKES_DESCRIPTOR: Final[EffectMask] = 1 << 15
INVOKES_ITERATION_CALLBACK: Final[EffectMask] = 1 << 16
INVOKES_CONTEXT_CALLBACK: Final[EffectMask] = 1 << 17
INVOKES_COMPARISON_CALLBACK: Final[EffectMask] = 1 << 18
RAISES: Final[EffectMask] = 1 << 19
SUSPENDS: Final[EffectMask] = 1 << 20
ALL_EFFECTS: Final[EffectMask] = 0x1FFFFF
UNKNOWN_EFFECTS: Final[EffectMask] = ALL_EFFECTS

EFFECT_ROWS: Final[tuple[tuple[str, EffectMask, str], ...]] = (
    (
        "executes_arbitrary_python",
        EXECUTES_ARBITRARY_PYTHON,
        "May execute arbitrary user-controlled Python code.",
    ),
    (
        "invokes_import_system",
        INVOKES_IMPORT_SYSTEM,
        "May invoke finders, loaders, import hooks, or module execution.",
    ),
    (
        "reflects_namespace",
        REFLECTS_NAMESPACE,
        "May dynamically observe or alter namespaces through reflection.",
    ),
    (
        "reads_module_metadata",
        READS_MODULE_METADATA,
        "Reads __name__, __package__, __spec__, or related module metadata.",
    ),
    (
        "writes_module_metadata",
        WRITES_MODULE_METADATA,
        "Writes or deletes __name__, __package__, __spec__, or related metadata.",
    ),
    (
        "reads_global_namespace",
        READS_GLOBAL_NAMESPACE,
        "Reads a module global namespace outside statically named loads.",
    ),
    (
        "writes_global_namespace",
        WRITES_GLOBAL_NAMESPACE,
        "Writes or deletes a module global namespace dynamically.",
    ),
    (
        "reads_frame_state",
        READS_FRAME_STATE,
        "Observes an executing frame or its locals/globals dynamically.",
    ),
    (
        "writes_frame_state",
        WRITES_FRAME_STATE,
        "Mutates an executing frame or its locals/globals dynamically.",
    ),
    (
        "reads_object_state",
        READS_OBJECT_STATE,
        "Reads object state through a path that may observe mutable identity.",
    ),
    (
        "writes_object_state",
        WRITES_OBJECT_STATE,
        "Mutates object state, including attribute or item storage.",
    ),
    (
        "allocates",
        ALLOCATES,
        "May allocate an identity-bearing Python or runtime object.",
    ),
    (
        "releases_reference",
        RELEASES_REFERENCE,
        "May release an owned reference and cross a zero-reference boundary.",
    ),
    (
        "runs_finalizer",
        RUNS_FINALIZER,
        "May run __del__ or another finalization callback.",
    ),
    (
        "runs_weakref_callback",
        RUNS_WEAKREF_CALLBACK,
        "May run a weak-reference callback.",
    ),
    ("invokes_descriptor", INVOKES_DESCRIPTOR, "May invoke descriptor protocol code."),
    (
        "invokes_iteration_callback",
        INVOKES_ITERATION_CALLBACK,
        "May invoke iterator, generator, or asynchronous iteration code.",
    ),
    (
        "invokes_context_callback",
        INVOKES_CONTEXT_CALLBACK,
        "May invoke context-manager enter or exit code.",
    ),
    (
        "invokes_comparison_callback",
        INVOKES_COMPARISON_CALLBACK,
        "May invoke comparison, hashing, or truth-conversion code.",
    ),
    ("raises", RAISES, "May raise or propagate a Python exception."),
    (
        "suspends",
        SUSPENDS,
        "May suspend or transfer control through yield, await, or scheduling.",
    ),
)

NO_PYTHON_CALLBACKS_FORBIDDEN_EFFECTS: Final[EffectMask] = 0x7E003
PRESERVES_IMPORT_STATE_FORBIDDEN_EFFECTS: Final[EffectMask] = 0x7E157
NO_NAMESPACE_REFLECTION_FORBIDDEN_EFFECTS: Final[EffectMask] = 0x1FC
NO_REFERENCE_RELEASE_FORBIDDEN_EFFECTS: Final[EffectMask] = 0x7F003
REFERENTIALLY_TRANSPARENT_FORBIDDEN_EFFECTS: Final[EffectMask] = 0x1FFFFF
CANNOT_RAISE_FORBIDDEN_EFFECTS: Final[EffectMask] = 0xFE003
CANNOT_SUSPEND_FORBIDDEN_EFFECTS: Final[EffectMask] = 0x130003

CAPABILITY_ROWS: Final[tuple[tuple[str, EffectMask, str], ...]] = (
    (
        "no_python_callbacks",
        NO_PYTHON_CALLBACKS_FORBIDDEN_EFFECTS,
        "Proves the operation cannot transfer control into Python or import hooks.",
    ),
    (
        "preserves_import_state",
        PRESERVES_IMPORT_STATE_FORBIDDEN_EFFECTS,
        "Proves source-order __name__/__package__/__spec__ state cannot change.",
    ),
    (
        "no_namespace_reflection",
        NO_NAMESPACE_REFLECTION_FORBIDDEN_EFFECTS,
        "Proves no dynamic module, global, or frame namespace access occurs.",
    ),
    (
        "no_reference_release",
        NO_REFERENCE_RELEASE_FORBIDDEN_EFFECTS,
        "Proves neither direct release nor callback-capable code can drop references.",
    ),
    (
        "referentially_transparent",
        REFERENTIALLY_TRANSPARENT_FORBIDDEN_EFFECTS,
        "Proves no observable Python effect; only the zero mask qualifies.",
    ),
    (
        "cannot_raise",
        CANNOT_RAISE_FORBIDDEN_EFFECTS,
        "Proves the operation cannot raise or transfer into code that may raise.",
    ),
    (
        "cannot_suspend",
        CANNOT_SUSPEND_FORBIDDEN_EFFECTS,
        "Proves the operation cannot suspend or transfer into scheduling code.",
    ),
)


def join_effects(left: EffectMask, right: EffectMask) -> EffectMask:
    """Least upper bound. Internal hot paths intentionally use one OR."""
    return left | right


def effect_mask_is_known(mask: EffectMask) -> bool:
    """Return whether *mask* contains only bits in this schema version."""
    return mask >= 0 and mask & ~ALL_EFFECTS == 0


def canonicalize_effects(mask: EffectMask) -> EffectMask:
    """Fail closed at untrusted/versioned boundaries: unknown bits become top."""
    return mask if effect_mask_is_known(mask) else UNKNOWN_EFFECTS


def effect_mask_has_any(mask: EffectMask, projection: EffectMask) -> bool:
    return bool(mask & projection)


def effect_mask_has_all(mask: EffectMask, projection: EffectMask) -> bool:
    return mask & projection == projection


def effect_mask_satisfies_capability(
    mask: EffectMask, forbidden_effects: EffectMask
) -> bool:
    """Capabilities fail closed for masks from a newer/invalid schema."""
    return effect_mask_is_known(mask) and mask & forbidden_effects == 0
