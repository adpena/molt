/*
 * Compatibility shim for NumPy generated source include.
 *
 * Molt's libmolt header surface provides declarations only for syntax-level
 * compatibility scans, so this file intentionally does not define runtime
 * tables.
 */
