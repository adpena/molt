# Native Op-Family Audit

## Structural Change

- Updated `tools/audit_op_kinds.py` so native SimpleIR coverage consumes the extracted native op-family routing authorities:
  - `function_compiler/fc/*::HANDLED_KINDS`
  - `fc/op_family.rs` inline dispatch slices
  - legacy `function_compiler.rs` textual arm scan as a compatibility backstop
- Added generator tests proving `guarded_field_init` is visible through the extracted `fc/memory.rs::HANDLED_KINDS` authority and restoring `test_guarded_field_init_has_one_wire_spelling`.
- Updated `docs/design/foundation/25_op_kind_registry.md` to describe the native audit source split.

## Verification

- `uv run --python 3.12 pytest -q tests/test_gen_op_kinds.py`
  - passed: 86 passed
- `uv run --python 3.12 python tools/audit_op_kinds.py --check`
  - passed: dangerous-cell baseline is exact
- `uv run --python 3.12 python tools/gen_op_kinds.py --check`
  - passed: generated files in sync
- `uv run --python 3.12 python tools/structural_audit.py --check`
  - passed: 495 findings; 4 metrics improved

## Notes

- This fixes the prior full `tests/test_gen_op_kinds.py` failure where `guarded_field_init` reported `native_arm=false` after native backend decomposition.
- Unowned native-backend, CLI, and differential-test WIP remains present and was left untouched.
