# Vectorize Generated Classification Report

## Structural Change

- Replaced vectorize.rs private opcode classifiers with generated
  `vectorize_opcode_facts`.
- Folded body eligibility, live Copy refinement dispatch, iteration-control
  skips, loop-header markers, annotation targets, and reduction-family
  membership into one exhaustive generated `opcode_vectorize_facts_table`.
- Removed the old reduction-only generated table path from live generation and
  production pass code.
- Updated spec routing docs so future work lands in `op_kinds.toml` instead of
  recreating pass-local opcode taxonomies.

## Proof

- `uv run --python 3.12 python tools/gen_op_kinds.py`
  - Regenerated `runtime/molt-tir/src/tir/op_kinds_generated.rs` and frontend
    op-kind output from the canonical table.
- `uv run --python 3.12 pytest tests/test_gen_op_kinds.py -k "generated_rs_is_in_sync or generated_py_is_in_sync or vectorize_opcode"`
  - 4 passed.
- `cargo test -p molt-tir vectorize`
  - 7 passed.
- `uv run --python 3.12 python tools/structural_audit.py --write-board`
  - Rewrote `docs/design/foundation/STRUCTURAL_AUDIT_BOARD.md`; current board
    shows `semantic_fallthrough (18)`.
- `uv run --python 3.12 pytest tests/test_gen_op_kinds.py`
  - 106 passed.
- `uv run --python 3.12 python tools/audit_op_kinds.py`
  - Exit 0; existing dangerous-cell summary remains outside this vectorize
    authority move.

## Notes

- `tools/agent_coordination.py proof-plan ...` returned no canned
  recommendations for the explicit path set, so the proof lane was chosen from
  the changed authority surface.
- The first structural-audit command used the stale `--board` spelling from the
  prior coordination plan. The current tool accepts `--write-board`; the
  coordination artifact now records the corrected lane.
