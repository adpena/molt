name: Perf Validation

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  benchmark:
    name: Benchmark Validation
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      MOLT_SESSION_ID: perf-validation
      MOLT_EXT_ROOT: ${{ github.workspace }}
      CARGO_TARGET_DIR: ${{ github.workspace }}/target
      MOLT_DIFF_CARGO_TARGET_DIR: ${{ github.workspace }}/target
      MOLT_CACHE: ${{ github.workspace }}/.molt_cache
      MOLT_DIFF_ROOT: ${{ github.workspace }}/tmp/diff
      MOLT_DIFF_TMPDIR: ${{ github.workspace }}/tmp
      UV_CACHE_DIR: ${{ github.workspace }}/.uv-cache
      TMPDIR: ${{ github.workspace }}/tmp
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version-file: ".python-version"

      - uses: astral-sh/setup-uv@v8

      - uses: dtolnay/rust-toolchain@stable

      - name: Sync Python environment
        run: uv sync --group bench --group dev

      - name: Run benchmarks
        run: python3 tools/guarded_exec.py --prefix MOLT_BENCH -- python3 tools/bench.py --molt-profile release --json-out bench/results/perf-validation.json

      - name: Upload benchmark artifact
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: benchmark-results
          path: bench/results/perf-validation.json
